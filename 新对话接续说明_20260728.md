# PI-JWM 新对话接续说明（2026-07-28）

> 用途：供下一周的新对话快速恢复项目上下文。新对话应先核对本文件所列原始文件和结构化产物，再提出计划或修改内容。
> 当前原则：区分“目标版理论定义”“已经实现”“已经实验验证”和“仍待完成”，不得把计划、受控夹具或诊断结果写成正式结论。

## 1. 项目当前主线

PI-JWM（Physical-Information Joint World Model，物理—信息联合世界模型）面向基础设施辅助的动态空地协同移动边缘计算网络。AirFogSim只是当前的参考仿真器和数据源，不是项目框架。

`基于信道图谱的RRM`是另一独立项目，不属于PI-JWM主线。其论文、信道建模方法和研究思路可以作为参考，但其代码、实验结果和项目结论不得直接归入PI-JWM；任何借鉴内容都需要在PI-JWM的数据契约、系统假设和实验协议下重新实现与验证。

当前目标链路为：

```text
当前观测与历史
→ 严格物理图 + 严格信息图 + 跨图关系
→ PI-JWM编码与动作条件联合动力学
→ 对候选卸载/RB/CPU动作进行多步rollout
→ 预测状态、任务结果和风险
→ 策略器/规划器比较候选
→ 安全壳检查后执行一个动作
→ 获得真实后继状态并继续滚动
```

世界模型不包含策略器。策略器负责生成候选动作；PI-JWM接收状态和候选动作，预测该动作的后果；规划器根据预测结果选择动作。

当前第一控制切片只联合控制：

- 任务卸载目标；
- 无线RB分配；
- 计算CPU分配。

车辆移动、UAV轨迹、发射功率、MCS、缓存和mission等暂不作为第一控制切片中的联合决策变量。后续可以扩展，但必须先形成新的数据、约束和消融证据。

## 2. 当前权威文档与固定框架

理论与研究框架的唯一主文档：

```text
D:\禹尧珅\人工智能知识库\北大科研\PIJWM\PIJWM推进.md
```

该文档当前固定为七个主体部分：

1. 研究场景与核心问题；
2. 严格双图联合系统模型；
3. 优化问题与学习目标；
4. PI-JWM总体方法；
5. 理论依据与创新分析；
6. 数据与实验验证；
7. 当前实现与后续推进。

第一至第六部分已经形成目标版定义和实验框架，第七部分记录当前实现证据及下一阶段任务。框架中的“目标版方法”不等于全部已经编码或训练完成。

其他入口：

```text
D:\shen\网络组\AGENTS.md
D:\shen\网络组\本地计划表.md
D:\shen\网络组\progress.md
D:\shen\网络组\findings.md
D:\shen\网络组\task_plan.md
D:\shen\网络组\代码\scripts\small_experiments\README.md
```

根目录README的状态日期较旧，只可参考目录和安装方式；最新研究口径以本交接、主推进文档和结构化实验产物为准。

## 3. 理论部分当前已经固定的核心口径

### 3.1 严格双图

- 物理图表示物理实体、可行通信/连接关系和物理属性。
- 信息图表示任务或信息实体及其有向依赖关系，必须是非空DAG才能用于验证信息图价值。
- 两张图的节点集合不是同一集合，不能仅靠“共享节点特征”称为严格双图。
- 跨图耦合由MN、ME、EP等显式关系表达：信息节点与承载物理节点的映射、信息生命周期对应的物理路径、信息依赖流对应的物理承载关系。
- 双图结构合法不等于双图对学习有效；“严格双图优于单图”仍需正式S1消融实验验证。

### 3.2 状态、动作与预测

目标版状态区分：观测、显式状态、隐式latent belief和不确定性。状态包含物理、通信、任务与资源信息，但只有数据中真实存在或可严格推导的字段才能进入当前实现；缺失字段必须使用显式掩码，不得用0冒充真实值。

PI-JWM不只预测链路速率。目标版输出包括：

- 物理/通信状态及链路活动与条件速率；
- 任务生命周期、剩余工作量和完成/失败事件；
- RB、CPU、队列和已建模能量等资源状态；
- 多步rollout结果；
- 概率分布、协方差或区间等不确定性；
- 服务于候选动作排序的任务级结果。

### 3.3 优化目标和评价

系统最终关注任务服务效用，而不是单独最小化link-rate RMSE。目标综合考虑任务完成、时延、能耗、资源利用、公平性和风险，硬约束必须零违规，软约束通过惩罚或风险项处理。

正式评价需分层报告：

- 状态/结构预测：连续误差、事件预测、图合法性和守恒残差；
- 概率与校准：NLL、Brier/CRPS、ECE、coverage与width；
- 动作评价：配对效果误差、排序指标和regret；
- 闭环系统：完成率、失败率、P95/P99时延、能耗、资源利用、公平性、拒绝/回退率；
- 泛化与部署：跨seed、负载、拓扑、OOD、外部真实留出和推理时延。

## 4. 本轮已经完成的训练前小实验

所有小实验代码位于：

```text
D:\shen\网络组\代码\scripts\small_experiments\
```

结构化结果位于：

```text
D:\shen\网络组\代码\artifacts\small_experiments\
```

| 编号 | 实验 | 当前结论 | 证据边界 |
| --- | --- | --- | --- |
| 00 | 严格双图合法性 | 受控夹具中的非空DAG、MN/ME/EP、路径、流守恒和规则级动作敏感性均通过 | 只证明表示和验证器可用，不是AirFogSim真实证据，也不证明双图优于单图 |
| 01 | 数据契约字段审计 | 64项字段中39项直接提供、8项可推导、17项缺失；旧数据不满足正式训练契约 | 审计完成不等于数据就绪；真实数据集当时只有文献线索 |
| 02 | AirFogSim严格双图预检 | 实际轨迹可导出非空DAG和部分MN/ME，但直接EP载荷与物理承载证据不足，`strict_dual_graph_ready=false` | 这是对当次导出接口和语义的判断，不是模拟器所有潜在接口的不存在证明 |
| 03 | AirFogSim跨图证据闭合 | 监听159个实际信道事件，形成158条ME路径、422条信息边/EP关系，12项严格检查通过，`strict_dual_graph_ready=true` | DAG载荷使用PI-JWM的`shared_parent_output`扩展语义，不是AirFogSim原生DAG载荷 |
| 04 | 任务与资源守恒 | 任务、依赖、RB、CPU、UAV能量及通信能量输入等守恒门全部通过，`conservation_ready=true` | 车辆/RSU能量、CPU计算能耗和完整存储仍未建模，不能填零 |
| 05 | 配对动作因果敏感性 | seeds 0/1/2下，3个卸载配对和3个RB配对均合法、生效且可重复，`action_sensitivity_ready=true` | 只证明动作确实改变后继状态，不证明动作更优，也不证明PI-JWM已学会该关系 |

实验03—05是当前主实验启动前最关键的直接证据。实验00—02仍应保留，因为它们记录了从“受控规则”到“实际仿真证据闭合”的问题发现过程。

## 5. 主实验收口的真实状态

主实验准备报告：

```text
D:\shen\网络组\代码\artifacts\main_experiment_readiness\readiness_v1\readiness_report.json
```

截至2026-07-26的机器可读状态：

| 状态 | 值 | 准确含义 |
| --- | --- | --- |
| `simulation_training_ready` | `true` | 可以启动正式仿真数据生成和训练smoke |
| `formal_dataset_ready` | `false` | 尚未生成并冻结满足契约的正式规模train/calibration/test数据 |
| `external_validation_ready` | `false` | 尚无本地可读取且完成语义核验的真实测量外部留出集 |

因此当前还不能宣称“正式PI-JWM主实验已经完成”。已经完成的是训练前理论定义、契约、验证器、仿真证据闭环和启动门收口。

## 6. 数据来源当前决策

- AirFogSim继续承担任务、资源、可执行动作和反事实轨迹的主仿真数据来源。
- 不能假定某个真实数据集同时包含任务、资源、信道和动作字段。
- 射线追踪/信道数据可用于传播侧校准和长尾补充。
- 真实无线测量或测试床数据应主要用于物理侧校准、域迁移和external holdout。
- 高保真联合网络仿真器可补充AirFogSim没有覆盖的协议、CSI或MAC细节。
- 具体替代/补充数据源尚未最终冻结；必须先完成字段、时间对齐、动作支持、许可和可复现性核验，再决定是否接入训练。

Zotero是PI-JWM文献的权威库，集合配置和去重规则写在`AGENTS.md`及文献管理说明中。综述用于确定路线，正式定义和论证应回到原始论文；不得把未核验的2025—2026预印本直接作为定论。

## 7. 下一周建议的主线顺序

新对话首先应让用户确认下一周侧重是“汇报整理”还是“正式训练推进”。若进入训练，建议按下列顺序执行，不应跳过数据冻结直接做大规模GPU训练：

1. 按已冻结的卸载/RB/CPU控制切片，生成新版正式规模严格双图仿真轨迹。
2. 对每条轨迹运行字段契约、缺失掩码、单位、配置哈希、MN/ME/EP、守恒和动作对齐检查。
3. 按完整seed/场景冻结train、validation、calibration和test，避免滑窗泄漏。
4. 使`formal_dataset_ready`由`false`转为`true`，同时保存机器可读报告和manifest。
5. 先运行CPU/GPU小规模训练smoke，确认loss、输出头、梯度、checkpoint和完整metric链路。
6. 首个正式实验优先做S1：严格双图、单图、双分支无耦合、打乱跨图关系的受控消融。
7. S1通过后再做`k=1,5,20`多步rollout、守恒、概率头/ensemble/共形校准和动作排序实验。
8. 外部数据单独完成字段与语义核验；在`external_validation_ready=true`前，不报告真实网络泛化结论。

如果下一周先准备汇报，则建议继续使用三部分结构：理论目标与metric重构、真实数据增强路线、训练前小实验与主实验准备状态。必须把“已完成的小实验”和“尚未启动的正式训练”分开表述。

## 8. 必须保留的研究与工程边界

1. AirFogSim是数据源，不是PI-JWM框架。
2. 世界模型负责动作条件预测，策略器/规划器负责候选生成与选择，二者不能混写。
3. v5/v11 selector和ranking属于后置决策诊断接口，不是当前PI-JWM世界模型的主方法。
4. 旧60-seed数据中的DAG概率为0，不能用它证明信息图或跨图耦合有效。
5. 实验03的DAG载荷是PI-JWM声明的扩展语义，必须如实标注，不得写成AirFogSim原生支持。
6. `simulation_training_ready=true`不等于正式数据、正式模型或论文级真实验证已经完成。
7. 不确定性头、共形校准、完整多步metric、正式S1消融和真实外部验证仍待完成。
8. matched test seeds 18—19曾按旧协议访问，后续不能用于调参；external holdout seeds 60—69仍保持锁定，除非新协议预注册并满足解锁条件。
9. 所有结论必须标注为`deployable`、`true_future_reference`、`sample_oracle`、`diagnostic_only`或`external_holdout`等准确口径。
10. 不覆盖用户已有改动，不执行`git reset --hard`或恢复当前归档删除。当前工作树有大量已知修改和未跟踪文件，应先运行`git status --short`再编辑。
11. 新增可复用代码放入`代码/src/pi_jwm/`，脚本放入`代码/scripts/`，测试放入`代码/tests/`，生成物放入`代码/artifacts/`；训练前小实验继续与主线模型代码分离。
12. 用户希望每一步都理解“为什么做、输入是什么、实际做了什么、结果意味着什么”。新对话不得只给命令或直接长时间训练。

## 9. 新对话开始时应先检查的内容

```powershell
cd D:\shen\网络组
git status --short
Get-Content -Raw -Encoding UTF8 .\新对话接续说明_20260728.md
Get-Content -Raw -Encoding UTF8 .\AGENTS.md
Get-Content -Raw -Encoding UTF8 .\本地计划表.md
Get-Content -Raw -Encoding UTF8 .\代码\artifacts\main_experiment_readiness\readiness_v1\readiness_report.json
```

随后查看主推进文档的第六、七部分，以及实验03—05的`REPORT.md`与`validation_report.json`。不要只依据聊天摘要判断项目状态。

## 10. 可直接用于下一对话的开场提示

```text
请先完整阅读 D:\shen\网络组\新对话接续说明_20260728.md、AGENTS.md、本地计划表.md、当前主实验readiness_report.json，以及 D:\禹尧珅\人工智能知识库\北大科研\PIJWM\PIJWM推进.md 的第六、七部分。先运行 git status --short，不要覆盖或恢复现有改动。

请先用自己的话向我说明：PI-JWM最终解决什么问题，严格双图是什么，策略器和世界模型如何分工，当前小实验00—05分别验证了什么，以及为什么 simulation_training_ready=true 但 formal_dataset_ready=false。然后再和我一起确定下一周目标。每一步都要先解释目的、输入、将做的操作和验收标准；不要默认继续旧v11 selector，也不要在未冻结正式数据前直接启动大规模GPU训练。
```
