# PI-JWM 本地计划表

更新时间：2026-07-20

## 2026-07-20 Token-level Selector 根因与 GPU 迭代

- 正式 task-only 训练已完成 12 配置 x 3 seed，但 validation 全部 100% defer，active-rate RMSE 等于 ranked baseline `233.7162`；matched test 18--19 未在本轮访问。
- 已用现有 36 checkpoints 完成 12 配置 x 6 决策规则归因：rank-only validation RMSE 为 `292.90--312.33`，所有 seed 均恶化，说明根因是候选排序失败，不是 defer 或 Pareto 门过严。
- schema-v6 interaction HGB 审计显示 opportunity 可识别（PR-AUC `0.9495`、ROC-AUC `0.8752`），但候选内排序仍弱（sample rank Spearman `0.0832`）；234 维手工池化不足以表达局部 edge-step 耦合。
- 已按 TDD 新增 `InteractionCandidateBenefitRanker`：直接编码 `72 x 25` edge-step tokens，分离 opportunity gate 与 candidate benefit ranking，并只在真实存在改进机会的样本上施加 listwise 排序损失。
- 新增正式 runner `代码/scripts/train_v11_interaction_selector.py`，只接受 train/calibration/validation schema-v6 缓存；calibration 决定 opportunity/sign 安全阈值，validation 选型，脚本不包含 matched-test 或 external-holdout 入口。
- 当前本地验证为新旧相关测试 `85/85` 通过，Python 编译与 diff 检查通过。下一步先在 RTX 4090 运行单配置 2 epoch 全数据 smoke，再扩大 token selector 网格；在 validation gate 通过前不得打开 external holdout 60--69。
- RTX 4090 全量 2-epoch smoke 用时约 42 秒，3-seed x 20-epoch 无 phase probe 用时约 206 秒；loss 正常下降，但 calibration 在 sign=0.50 时仅 43.9% 正收益精度、sign>=0.65 时全回退，因此无 phase token 版本未通过。
- `sample_id mod 390` 可在不使用 seed 身份的情况下恢复当前 episode step。将其四维相位编码加入 full schema-v6 HGB 后，validation RMSE 达 `232.0230`，6 次执行全部为正收益，证明任务演化位置是有效的缺失上下文。
- episode phase 已按 TDD 接入正式 token runner，相关测试更新为 `86/86` 通过。下一步保持其他协议不变，重跑 phase-aware token probe，严格归因该特征的增益。

## 2026-07-20 Selector GPU 训练前冻结

- 完整 schema-v6 crossfit 标签已存在，无需重复生成：train `15,600`、calibration `2,340`、validation `3,900`；正式 validation candidate gate 为 sample-oracle RMSE `105.3486`、nontrivial `83.69%`、identity oracle-win `25.75%`、action-applied `100%`，四项全部通过。
- h=10 `causal_policy_v1` 物理标签完成：train 120 组/809 候选/8,090 step rows，calibration 18 组/117 候选/1,170 rows；train 非零 task/energy 效应分别为 `265/689` 与 `94/689`，明显高于 h=3 的 `158/689` 与 `35/689`。
- 物理 bridge 冻结为 `task_only`：task OOF MAE `0.16023 < 0.16310`，calibration MAE `0.07914 < 0.08176`，coverage `92.86%`；energy 模型未超过基线，真实 UAV energy 严格保留为 `audit_only`，在线 Pareto 使用 physical task LCB + deployable energy proxy。
- 64-train/64-validation 的真实 task-only 缓存 CPU 闭环通过：32 候选、75 维特征，1 epoch loss/预测均有限、Pareto 维度一致且所有选择合法。`defer_ratio=100%` 仅是 1 epoch 执行链 smoke，不作为性能结论；全量 CPU 基线/训练超过 30 分钟，正式网格应使用 GPU。
- 正式数据 split 再次复核无交叉；matched test `18--19` 和 external holdout `60--69` 均未访问。GPU 训练只读取 train/calibration/validation，validation 冻结前不得运行 feature ablation 或打开任何锁定集。
- GPU 路线现已收敛为单一步骤：运行 `代码/scripts/run_v11_candidate_set_selector_gpu.sh`。脚本固定使用 h=10 task-only caches、physical bridge manifest、12 个配置 × 3 个训练 seed，并要求 CUDA、tracked tree clean 和输入 SHA-256 可追溯。
- 旧 `run_v11_objective_aligned_selector_gpu.sh` 仍是历史 schema-v5 对照入口，不再作为本轮正式路线。正式路线使用 `CandidateSetBenefitRanker`，world model 和 policy checkpoint 均冻结。
- GPU 交接证据位于 `代码/artifacts/reports/pi_jwm_v11_gpu_handoff_20260719/`。只有源码提交后重新审计得到 `ready_for_gpu_selector_training` 和 `selector_training_allowed=true` 才允许启动；当前仍不得宣称 selector 定型或 RMSE 已突破 200。

## 项目主线

PI-JWM 面向网联具身智能体协同场景，构建物理网络-信息网络联合世界模型：

1. 从历史节点、链路、任务、资源和动作构造物理图与信息图。
2. 学习动作条件下的多步状态转移。
3. 预测未来节点、链路活动/速率、任务与资源状态。
4. 在冻结世界模型中评价自主动作策略。

AirFogSim 只负责参考仿真与数据生成。当前决策方法是 `v11 candidate`，不是 v12，也不是最终 v11。

## 2026-07-19 Physical Benefit Bridge 本地门控结果

- 已完成 leakage-safe `PhysicalBenefitBridge` 软件链：sample-time 精确对齐、selector-timed step-1/2 干预、paired task/energy 标签、五折 seed-held-out RF ensemble、split-conformal 区间、八个 `physical_*` 可观测特征、task LCB/energy UCB Pareto 回退、schema-v6 cache 保留与 selector manifest/hash 门禁。
- seeds 0--4 的真实 AirFogSim smoke 覆盖 15 个决策组、96 个候选和 288 个 step rows；15/15 组 step 0 一致，缺失值、负能耗、reward 重建错误和能耗守恒错误均为 0。
- formal 时序支持未通过：81 个非默认候选中只有 3 个 CPU 候选在共享 step 0 后仍生效，RB/offload/mixed/return-route 生效数均为 0；3 个有效 CPU 候选的 task/energy/throughput 效应也全部为 0，因此有效非平凡组为 `0/15`。
- 当前冻结为 `physical_bridge_not_identifiable / diagnostic_only`。没有扩大 formal train/calibration 物理标签，没有拟合正式 bridge，没有生成 augmented formal caches，也不启动 GPU selector；matched test 和 external holdout 均未访问。
- 结果包位于 `代码/artifacts/reports/pi_jwm_v11_physical_benefit_bridge_20260719/`。下一步必须先把 formal candidate 改成跨 step 可执行且不读取未来真值的闭环动作抽象，再重新验证其与 PI-JWM step-1/2 action 的语义对应；不能通过增加 seed 或放宽 action-applied 门槛绕过。

## 2026-07-19 时序可执行候选协议结果

- 已将 formal 候选从绝对 task-ID 动作重构为 `causal_policy_v1`：决策时冻结 family、scale、coverage、rank 和时间模式，step 1/2 只按当前合法动作集合解析；旧 `precomputed_task_v0` 路径保留用于历史复现。
- 新增 `action_applicable`、`action_supported`、`action_changed` 三层审计语义，安全 no-op 不再被误记为非法 mutation；正式标签仍单独统计支持覆盖和真实状态变化。
- 决策点选择改为优先覆盖量最大的 offload/RB、compute、return 阶段，避免早期弱支持点掩盖候选可辨识性。
- 5-seed smoke（0--4）完成：15 个决策组、101 个候选 rollout、303 个 step rows；非默认候选 86/86 安全可执行，36 个获得实际支持，28 个改变环境状态，8/15 组非平凡。
- 缺失值、负能耗、reward 重建、能耗守恒和非法动作错误均为 0；matched test 18--19 与 external holdout 60--69 均未访问。
- 结果状态为 `ready_for_formal_label_generation`，PPT 底稿位于 `代码/artifacts/reports/pi_jwm_v11_temporal_candidate_protocol_20260719/ppt_evidence.md`，主对比图为 `figure_0_protocol_breakthrough.png`。
- 当前仍不启动 GPU selector 训练；下一步先生成 formal train/calibration actual-rollout 标签并运行 bridge candidate-oracle 门，只有该门通过后才允许 GPU。

## 2026-07-18 Objective-Aligned Selector GPU 交接

- 本地实现已完成：目标与全局 active-rate SSE 对齐的 `OpportunityHead + CandidateBenefitHead`、impact weighting、ensemble uncertainty、calibration-only opportunity threshold、candidate LCB、task-energy Pareto 回退、安全 checkpoint、XGBoost 对照和 7 项固定消融均已接入；世界模型与 schema-v5 actual-rollout 标签保持冻结。
- 正式 runner 为 `代码/scripts/train_v11_objective_aligned_selector.py`；它只接受 train/calibration/validation 三份缓存，没有 matched-test 或 external-holdout CLI，也不会生成标签或重新训练 PI-JWM world model。
- 正式 GPU 入口为 `代码/scripts/run_v11_objective_aligned_selector_gpu.sh`。入口固定搜索 hidden dimension `{64,128}`、impact weight cap `{5,10}`，每个配置使用训练 seeds `17,29,41`、200 epochs，并运行 XGBoost 对照与固定消融。
- 现有 schema-v5 输入缓存及 SHA-256：train `fa90a82cb0d3392f7844008b0fc59c24ca1e91c4a066cc0b629262ef8245ffcc`；calibration `435a4cad70bb4e3fa4ca8aaa28ca3075393df92739625d77d33b1a1d97774064`；validation `e2178724fc9678de641c0f741b90d8f4df8b538d03e39e268dd3ee890ab576cb`。
- 本地 smoke 位于 `代码/artifacts/reports/pi_jwm_v11_objective_aligned_selector_20260718/local_smoke64/` 和 `local_smoke256/`；64/256 样本候选门均通过。小样本仅验证执行链，不作为指标结论；5-epoch 256-sample ensemble 仍 100% defer，符合“不得用 smoke 宣称突破”的口径。
- 两次完全同配置的 seed-17 CPU 重跑得到相同 checkpoint SHA-256 `8fc074f4d2c1e309393be5d397b2a46a633330209608362d6614a4781cfdd231`，`decision_trace_validation.csv` 与 `opportunity_calibration.csv` 也逐文件一致；三个 smoke checkpoint 均已通过 `weights_only=True` 加载。
- 本地质量门：`代码/tests` 602 项、`代码/scripts` 75 项测试通过；Python 编译、`bash -n` 和 `git diff --check` 通过。objective-aligned 实现基线 commit 为 `73da945`。
- 锁定边界：本轮所有 runner/smoke 的 `matched_test_accessed=false`、`external_holdout_accessed=false`；原 matched test 不再打开，external seeds 60--69 只有 validation 达到完整成功门后才允许一次性评价。
- 正式命令 `cd 代码 && bash scripts/run_v11_objective_aligned_selector_gpu.sh` 已于 2026-07-18 在 RTX 4090 执行完成；输出已下载到 `代码/artifacts/reports/pi_jwm_v11_objective_aligned_selector_20260718/selector_training/`。
- 成功门保持不变：validation active-rate RMSE `<200`、三个训练 seed 的 RMSE 标准差 `<=5`、至少 7/10 validation seeds 优于各自 baseline、activity F1 下降 `<=0.002`、link RMSE 相对恶化 `<=2%`。未同时通过时只能标记 `diagnostic_only`，不得打开 external。

### 2026-07-18 正式 validation 结果

- 正式运行绑定源码 commit `1e47a62490e37286205b3b159bc7027391240c0e` 和三份已冻结 schema-v5 缓存；运行耗时 `232.29` 秒，12 个主 checkpoint、7 项消融和 24 项全文件 SHA-256 清单均已生成并完成本地逐文件复核。
- candidate gate 继续通过：sample oracle RMSE `105.9892`、nontrivial ratio `83.33%`、identity oracle-win `24.18%`、action-applied `100%`。因此失败不是候选库没有上限。
- `h64/h128 × weight cap 5/10` 四个配置全部 100% defer，validation active-rate RMSE 均为默认 ranked allocation 的 `234.2529`，improved seed 为 `0/10`；最终状态为 `failure`、`configuration_frozen=false`、`diagnostic_only`。
- 关闭 opportunity 仍 100% defer；关闭 uncertainty 后 defer 降到 `63%`，但 RMSE 恶化为 `255.0580`。stage/task/resource/energy 和 uniform-impact 消融均未改变 100% defer 结论，说明单独放宽回退或移除某一组耦合特征不能解决问题。
- 强制执行模型 proposed candidate 会将 RMSE 恶化到 `264.2259`；有效样本中只有 `32.19%` 的提议产生正 SSE benefit，`42.70%` 产生负 benefit，总 SSE benefit 为 `-28,357,790`，预测 benefit 与真实 proposed benefit 的相关系数仅 `-0.0253`。10 个 validation seeds 中仅 seed 55 的强制提议总 benefit 略为正。
- candidate LCB 正值比例为 `0%`，opportunity gate 单独通过比例为 `8.80%`，两门同时通过为 `0%`。这证明 uncertainty defer 起到了正确止损作用，根因仍是 candidate benefit 排序/回归不可辨识，而不是 calibration threshold 太严。
- XGBoost 在服务器环境中不可用，按冻结协议记录为 `skipped_dependency_unavailable`，没有临时安装依赖或改变正式口径。
- matched test 没有再次访问，external seeds 60--69 仍未访问。当前两阶段 objective-aligned 结构必须停止，不得通过放宽 defer、追加超参网格或打开 external 继续追分；下一轮应先重新证明候选收益在测试时可观测特征上的可辨识性，再设计 selector。
- 已确认下一阶段采用“候选收益可辨识性优先”路线，设计见 `文档/项目说明/2026-07-18-PI-JWM候选收益可辨识性审计设计.md`。先复用冻结 schema-v5 缓存在本地 CPU 上审计 opportunity、benefit sign 和 conditional ranking；只有 validation 严格优于 best fixed `230.8556`、至少 7/10 seeds 改善且执行候选正收益 precision `>=65%`，才进入新的 GPU selector 训练。

### 2026-07-18 schema-v5 候选收益可辨识性正式审计

- 正式本地 CPU 审计已完成 6 个特征组 × linear/RF/HGB/XGBoost 共 24 个组合，结果位于 `代码/artifacts/reports/pi_jwm_v11_candidate_benefit_identifiability_20260718/formal_cpu/`，绑定源码 `50ddd29` 和三份 schema-v5 cache SHA-256；matched/external 均未访问。
- 最优 `full_schema_v5__rf` 的 validation active-rate RMSE 为 `233.9031`，默认 ranked-allocation 为 `234.2529`，未超过 best fixed `230.8556`。它只执行 3/1,398 个有效样本，全部集中于 seed 50 的 compute 阶段，只改善 1/10 seeds；当前分类为 `not_identifiable`，不得启动新的 GPU selector 训练。
- full-schema RF 的 candidate-sign PR-AUC/ROC-AUC 为 `0.4524/0.6721`，benefit Pearson 为 `0.1992`，但 sample 内排序 Spearman 只有 `0.0862`。现有全局摘要可以弱识别 opportunity，却不能稳定区分同一样本内候选次序。
- 下一步转向 schema-v6 局部 `edge × rollout-step` interaction 特征，保留被修改边的 current state、action delta、PI-JWM predicted response 与 stage/family 关系。只有简单模型先达到 sample 内 Spearman `>=0.20`、RMSE `<230.8556` 和至少 7/10 seeds 改善，才恢复 GPU selector 训练。
- schema-v6 路线已确认，正式设计见 `文档/项目说明/2026-07-18-PI-JWM-schema-v6局部Edge-Step交互设计.md`：每个候选固定 72 个 edge-step token（现有正式 split 最大 67），每个 token 25 维；同时生成 234 维 step × action-channel 可解释聚合。先完成本地 64/256 标签 smoke，完整 schema-v6 actual-rollout 标签生成才是下一次需要 GPU 的停止点。

### 2026-07-18 schema-v6 局部 Edge-Step 本地闭环

- schema-v6 已完成实现：每个被修改的 `(rollout step, edge)` 生成 25 维 token，固定容量 72；token 可重建 default/candidate action 与 PI-JWM default/candidate response，并汇总为 `3 step × 6 action channels × 13 statistics = 234` 维可解释特征。schema-v1--v5 loader 保持兼容，schema-v6 缺 token/mask/edge-index/pooled 任一字段均硬失败。
- 真实 64-sample train/calibration/validation CPU smoke 及完全同配置复跑已完成，运行时间分别为 `175.12/155.12` 秒；三份 NPZ 的文件级 SHA-256 和数组级规范哈希逐一相同，0 个字段不一致。三 split 最大 token 数分别为 `26/30/24`，overflow 均为 `0`。
- 真实 256-sample CPU smoke 用时 `365.83` 秒，三 split 最大 token 数为 `28/32/32`，仍远低于容量 72，overflow 均为 `0`。validation sample-oracle 为 `71.2723`，nontrivial ratio `69.23%`、identity oracle-win `53.85%`、action-applied `99.72%`；因后两项固定门要求 nontrivial `>=70%` 且 action-applied `100%`，该 smoke candidate gate 未通过，不能作为正式候选库结论。
- 256-sample 审计已完成 schema-v5/full、interaction-only、schema-v6/full × linear/RF/HGB/XGBoost 共 12 个组合，三折 group-CV 全部有效。四个 schema-v6 模型的 sample-rank Spearman 均超过 `0.20`（`0.2562/0.2883/0.2999/0.2778`）；其中 v6-RF candidate-sign PR-AUC `0.7201`，高于同口径 v5-RF `0.6885`。这说明局部耦合特征含有可辨识信号，但不是已定型结论。
- 当前 smoke winner 仍是 `full_schema_v5__xgb`，sample-rank Spearman `0.3523`、PR-AUC `0.6660`；安全规则只执行 5 个候选，正收益 precision `60%`、负选择率 `40%`、只改善 `2/10` validation seeds，因此总分类仍为 `not_identifiable / diagnostic_only`。不能用小样本绝对 RMSE 或某一特征指标宣称 selector 突破。
- 本地停止点已到达：下一步只在 GPU 上生成完整 train `15,600`、calibration `2,340`、validation `3,900` 的 schema-v6 actual-rollout 标签；入口为 `代码/scripts/run_v11_schema6_labels_gpu.sh`，它不训练 selector、不访问 matched/external，只在 CUDA、源码 clean、checkpoint、seed、schema、overflow、配置 digest 和 cache SHA 全部通过后交付标签。
- 本轮新增与回归测试已通过：`代码/tests` 共 `636` 项，`代码/scripts` 共 `75` 项；GPU 脚本 `bash -n`、Python 编译和 schema-v6 专项测试均通过。完整 schema-v6 标签尚未生成前，不启动正式 selector 训练，也不报告正式 validation RMSE。

### 2026-07-19 Selector Helper 按 Seed 交叉拟合正式结果

- 已完成五折 seed-crossfit 标签协议：40 个 selector train seeds 按固定 round-robin 分为五个 held-out folds，每个 train sample 的 score/value helper 均未读取该 sample 所属 seed；calibration/validation 只使用 40 个 train seeds 拟合的 final helper。正式标签为 train `15,600`、calibration `2,340`、validation `3,900`，五个 fold provenance 完整，三 split token overflow 均为 `0`，configuration/crossfit digest 各唯一。
- helper 样本内泄漏的机制问题已被消除：旧 in-sample train 中 persistent value-head 候选最多改善 `40/40` seeds，OOF train 降为 `18/40`。旧 train 上约 `154.929` 的异常固定候选 RMSE 不再出现；OOF train 的前列候选变为 `historical cap105/cap110` 和 decayed repair，与 calibration/validation 的候选方向明显更一致。
- 候选库仍有充分上限：validation sample-oracle RMSE `105.3486`、nontrivial ratio `83.69%`、identity oracle-win `25.75%`、action-applied `100%`，candidate gate 通过。固定候选中 cap105 为 `230.193`、改善 `6/10` seeds；cap110 为 `231.503`、改善 `7/10` seeds，说明单个固定候选不能同时满足 RMSE 与 seed 稳健性门。
- 正式 schema-v6 可辨识性审计仍为 `not_identifiable / diagnostic_only`。winner 为 `interaction_pooled_only__hgb`：validation RMSE `233.5156`（默认 `233.7162`）、改善 `2/10` seeds、执行 13 个候选、正收益 precision `46.15%`、负选择率 `15.38%`、sample-rank Spearman `0.0844`。交叉拟合显著降低了旧协议的负选择率，但没有形成稳定的候选内排序能力。
- 预注册 `hidden {64,128} × temperature {0.1,0.25,0.5} × dropout {0,0.1} × train seed {17,29,41}` 共 36 个 checkpoint 已在 RTX 4090 完成。最佳配置 `h128_t0.1_d0` 仍 `100% defer`，validation RMSE `233.7162`、改善 `0/10` seeds；七项冻结门中 RMSE `<230.8556` 和改善 `>=7/10` seeds 两项失败，其余 precision、负选择率、F1、link RMSE 和训练 seed 稳定性门通过。
- 最终状态为 `configuration_frozen=false`、`validation_not_frozen`，selector 继续标记 `v11 candidate`。按协议未运行 feature ablation、external holdout 或 matched test，不通过放宽 defer、增加超参或读取 locked split 追分。
- 正式结果位于 `代码/artifacts/reports/pi_jwm_v11_selector_helper_seed_crossfit_20260719/`。本地已保存 merged train/calibration/validation 缓存并逐文件复核 SHA-256；根目录 `summary.json` 汇总标签协议、候选偏移、可辨识性和 validation gate，`sha256_manifest.json` 覆盖除可变运行日志外的 177 个结果、checkpoint 和复现命令文件。
- 下一步停止继续扩大当前 CandidateSetBenefitRanker。研究重点应回到测试时可观测的候选 benefit 信号：优先检查 PI-JWM rollout 对局部 action-response 差值的保真度、calibration 上 improvement LCB 的可分性，以及是否需要直接预测 paired delta/风险而非从绝对 future state 间接恢复；在新的 validation 证据出现前不再打开 locked splits。

## 已确认基线

| 类型 | 结果 | 状态边界 |
|---|---:|---|
| v10 真实未来动作参考 active-rate RMSE | 100.140207 | `true_future_reference`，不是自主结果 |
| v10 自主 bridge 参考 active-rate RMSE | 217.237962 | deployable 历史参考 |
| ranked allocation active-rate RMSE | 213.160874 | deployable 候选参考 |
| expanded-55 strict-512 test oracle | 215.394402 | sample/test oracle，只证明候选 headroom |
| expanded-55 strict-512 deployable test best | 227.017662 | 测试事后最优，不是 val-selected 结果 |
| expanded-55 best-val matched test | 235.423373 | 当前严格匹配结果，未过线 |

## 当前判断

- 世界模型主干不是当前首要瓶颈；给定有效动作时状态预测明显更好。
- 主数据集 link-step 活跃率约为 0.0516%，支持集学习是核心难点。
- 独立 value bin、coupled token、hierarchical token 和简单 threshold/quota gate 均未稳定解决动作幅值与候选选择问题。
- actual-rollout 候选库存在 headroom，但验证选择不稳定；下一步不能继续只调 `top-k/alpha/cap`。
- `train_val` 拟合后再在同一 val 上选 operating point 必须标为诊断，不能作为 deployable 验证结论。
- 2026-07-13 的 UAV 能耗/逐步 reward 诊断覆盖 seeds 0--4、15 个配对决策组和 66 个候选；只有 7/15 个组具有非零 task-utility spread，候选可辨识性是 selector 之前的直接瓶颈。
- rollout horizon 扩展到 3/10/20 步后，非平凡决策组始终为 7/15（46.67%）；CPU-scale 的 12 个候选和 return-route 的 4 个候选仍无可见配对效应。由于本协议只在第 1 步注入候选动作、后续统一恢复默认调度，该结果证明的是当前一次性扰动接口缺少可辨识性，不能外推为 CPU/return-route 长期无效。
- 能耗惩罚会改变候选排序，但不同 lambda 的 val/test 方向不一致；能耗权重目前只能做敏感性分析，不能按测试表现事后选择。
- 2026-07-13 已修复诊断吞吐量重复计数：逐步 $\Delta B$ 仅取 AirFogSim 全局累计通信量 `channel.data_size` 的差值，不再与 V2I/V2U/U2I 子项相加；3/10/20 步结果已全部重跑并重新冻结，7/15 的结构性结论不变。

## 当前工程状态

- 2026-07-11 项目治理已完成；结果见 `文档/工程治理/2026-07-11-PI-JWM项目治理结果.md`。
- 项目环境由根目录 `pyproject.toml` 管理。
- 扩展 seed 数据必须显式指定 train/val/test split。
- 训练 preflight 会拒绝 action/target horizon 或 edge 维错位。
- v10/v11 新输出包含机器可读 `result_protocol`。
- selector 的纯 SSE/RMSE/动作混合逻辑已下沉到 `pi_jwm.evaluation`。
- WaveFarer 软件、隔离资料和数据集构建任务已按用户要求永久清理；8 个系统占用字体已登记为下次 Windows 启动前删除。
- artifacts 已建立保留/隔离/引用索引：456 个实验保留，12 个空或无证据残留进入隔离区。
- 最新全量验证为 659 项主测试和 75 项脚本测试通过；selector helper crossfit 正式结果清单 177 个文件 SHA-256 全部一致；3/10/20 步实验的质量审计全部通过，冻结清单 22 个文件 SHA-256 全部一致；两份研究进展文档均经 XeLaTeX 两遍编译和 PDF 可视检查。
- UAV 能耗、逐步 reward、配对归因和质量审计已下沉到 `pi_jwm`；AirFogSim 仍只作为仿真器和数据源，未修改其调度语义。
- 基础能耗诊断位于 `代码/artifacts/reports/pi_jwm_energy_reward_diagnostic_20260713/`；3/10/20 步冻结对照位于 `代码/artifacts/reports/pi_jwm_energy_reward_horizon_study_20260713/`。结果均统一标为 `diagnostic_only`，后者带 SHA-256 冻结清单和完整复现命令。
- v11 selector 定型链已实现 40/6/10-seed train/calibration/validation 协议、matched-test/external-holdout 双锁、32 候选 actual-rollout 标签、schema-v5 可审计缓存、DeepSets listwise benefit/risk ranker、ensemble LCB defer、feature-group ablation 和一次性冻结测试入口。seeds 18--19 已在首次冻结后访问一次并写入账本，后续 refinement 禁止再用；external seeds 60--69 仍未访问。
- 256-sample CPU smoke 中，32 候选 sample-oracle active-rate RMSE 为 71.2723，identity oracle-win 为 53.85%，候选动作生效率为 100%；非平凡比例 69.23% 距固定 70% gate 仅一个有效样本，因此该结果只证明值得扩大验证，不能代替 full validation。
- 本轮 selector 专项测试已覆盖动作适用/生效双掩码、逐样本 RMSE 标签、排序—增益—不确定性一致性、冻结 digest、checkpoint SHA、访问账本、外部验证和 AirFogSim 物理安全证据；schema-v4 的 8-sample actual-rollout 与完整 train/calibration/validation 软件链 smoke 均已跑通。
- 正式冻结文件会绑定候选/特征顺序、三份标签缓存、唯一超参数、checkpoint、world/policy checkpoint、源码 commit 与 defer/Pareto 规则；A 级结果不能通过手填 external wins 或安全违规数获得。
- 2026-07-16 新 RTX 4090 服务器已完成正式闭环。修正 applicability 后，full validation candidate gate 为 oracle `105.1993`、nontrivial `83.05%`、identity-win `24.82%`、action-applied `100%`，四项全部通过；随后生成 train `15,600`、calibration `2,340`、validation `3,900` 个样本的 32-candidate actual-rollout 标签并训练 12×3 selector。
- 首次冻结 selector 在 validation 上全部 defer，RMSE 等于默认动作 `233.9440`；冻结后的一次性 matched test 为 `223.8891`，defer `100%`，未达到 A/B 级，matched-test 访问已写入账本，不能再用于后续调参。相同 matched-test 的 sample oracle 为 `99.9032`，说明候选上限充足，失败来自 selector 可辨识性而非候选上限。
- 根因诊断只使用 train/calibration/validation：当前 46 维候选特征没有标准化，且 `context` 只是 identity 预测摘要，并未包含真实可观测 current state；全网均值还会把 8--32 条被修改边的变化稀释。标准化与 current-state 全局统计虽将 rank-only validation 从 `301.48` 改善到 `259.05`，仍不足。下一轮固定为补充 selected-edge 条件耦合特征、训练统计归一化和更充分优化，不放宽 defer，也不再查看 matched test；新结构最终只能用尚未访问的 external seeds 60--69 做一次独立验收。
- schema-v5 refinement 已在 RTX 4090 完整执行，且只访问 train/calibration/validation。validation candidate gate 为 oracle `105.9892`、nontrivial `83.33%`、identity-win `24.18%`、action-applied `100%`；但冻结 ranker 的 RMSE 为 `234.2529`、defer `99.974%`，与 ranked baseline 相同。rank-only 为 `268.1722`，去掉 uncertainty 后为 `241.2321`，证明失败来自排序学习而不只是回退阈值；external seeds 60--69 因此保持锁定。
- validation 最佳固定候选 `historical_ranked_cap105` 仅为 `230.8556`，而 sample oracle 为 `105.9892`。可实现 SSE 收益高度集中：top 10% 有效样本贡献 validation oracle 总收益的 `55.32%`，当前等样本 listwise loss 与最终 `sqrt(sum SSE / sum active_count)` 指标存在明确权重错配。下一方法变更须先在 train/calibration/validation 上验证目标对齐，validation 未低于 `200` 前不打开 external holdout。
- 8-sample schema-v5 CUDA 同配置复跑具有相同 configuration digest、candidate gate 和逐元素一致的 active SSE；CUDA graph `index_add_` 使候选特征最大差 `0.001953125`、全链路 SSE 最大差 `1.0`，因此当前证据是数值稳定而非 bitwise deterministic。复现文件遗漏非默认 `--rf-trees` 的缺陷已由回归测试捕获并修正为完整参数序列化。

## 下一步研究任务

### P0：提高 actual-rollout 候选可辨识性

- 已完成 3/10/20 步同候选集对照；单纯延长观察窗口未提高非平凡组比例，因此不再继续扩大同类 horizon。
- 下一轮改为构造持续多步候选序列，并按 offload/RB、compute、return-route 阶段分层选择真正具有对应任务状态的决策点。
- 为 compute/return 候选增加动作实际生效、资源状态变化和任务阶段转移检查；候选没有改变环境状态时保留并标记无效原因，不参与排序指标。
- 继续同时报告任务、链路、RB/CPU 和 UAV 能耗原始量，不以单一综合 reward 替代分项指标。
- 非平凡组比例和跨 seed 方向一致性未提高前，不继续横向尝试 selector 结构。
- 当前固定 32 候选已经通过本地小样本的 oracle 上限、identity 占比和动作生效检查；下一质量门只运行 full validation seeds 50--59，必须达到 oracle `<190`、nontrivial `>=70%`、identity-win `<65%`、action-applied `100%` 才允许正式 selector 训练。

### P1：PI-JWM rollout-aware candidate selector

- 候选支持充分后，使用 train actual-rollout 标签拟合 candidate-level/listwise selector。
- 预留独立 calibration/validation split，测试集只做一次匹配评价。
- 特征只使用测试时可获得的状态、policy 输出、PI-JWM forecast、不确定性和候选摘要，不使用 seed 身份或 simulator outcome。
- 同时报告 active-rate RMSE、activity precision/recall/F1、link/node/task RMSE 和能耗/reward 分项。
- 主方法已收敛为 `CandidateSetBenefitRanker`：listwise regret + pairwise margin + worst-stage group + calibration，三个训练 seed ensemble；结构仍标记为 `v11 candidate`，只有独立 validation 冻结后 matched test `<200` 且稳健性/安全门全部通过才能定型。
- schema-v5 已否定当前等权 listwise 版本。下一候选设计是目标对齐的两阶段 benefit/risk selector：先识别具有实质 SSE 收益的 opportunity，再在条件样本中按可实现 SSE 收益加权排序；权重只来自训练标签，测试输入仍只使用 current state、PI-JWM forecast 和候选摘要。该方法需单独确认并冻结新设计后实施。
- 当前正式 GPU 批处理入口为 `代码/scripts/run_v11_objective_aligned_selector_gpu.sh`；脚本只复用既有 schema-v5 train/calibration/validation 缓存，完成四配置三训练 seed、XGBoost 对照和固定消融，不生成标签，也不访问任何锁定 split。
- GPU 流水线先单独生成 validation 标签并检查 candidate gate，只有过线后才生成完整 train/calibration 标签，避免候选库上限不足时浪费完整标签算力。
- 外部 seeds 60--69 与真实 AirFogSim task-energy 安全审计是 A 级冻结的独立必要条件；代理能耗只用于测试时 Pareto 回退，不能冒充实际 UAV 能耗。

### P2：动作结构建模

- 将支持集选择与 RB/CPU 幅值重建分开评价。
- 保持 RB count-total、CPU count-total 等耦合约束。
- 优先验证候选生成质量，再决定是否扩大 GPU 训练。

### P3：跨场景与基座预训练

- 当前代码尚未完成真正的多场景预训练与少样本迁移。
- 在自主 bridge 稳定前，不把“基座模型”写成已实现结论。

## Stop Rules

- 没有 CPU/小样本 actual-rollout 证据，不启动长 GPU 实验。
- 只改善 token/value accuracy、但 frozen bridge active-rate RMSE 变差时停止。
- 小切片改善但 strict-512 不复现时，不扩大相同方法。
- test-best、sample oracle、true-future 结果不得升级为自主方法结论。
- 每次实验记录命令、seed、数据 split、输出路径、val 选择和 matched test。

## 工程治理

治理设计、实施计划和结果位于 `文档/工程治理/`。隔离区暂不永久删除；117 个含结构化证据的 smoke/probe 目录保留为 `manual-review`。逐次实验细节写入各实验目录的结构化 summary，不再追加到本计划表。
