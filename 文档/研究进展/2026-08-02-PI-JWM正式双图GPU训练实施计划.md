# PI-JWM正式双图GPU训练实施计划

> **执行要求：** 按TDD顺序完成；只同步非锁定张量；远端使用独立目录，不覆盖历史实验。

## 任务1：GPU协议与设备搬运

- [ ] 新建`代码/tests/test_run_formal_dual_graph_gpu_train_v1.py`，先验证CUDA设备约束、split白名单、递归张量搬运和锁定测试拒绝。
- [ ] 运行测试并确认因GPU入口不存在而失败。
- [ ] 新建`代码/scripts/run_formal_dual_graph_gpu_train_v1.py`，实现最小协议检查和递归设备搬运。
- [ ] 运行新增测试及45项正式CPU回归测试。

## 任务2：GPU训练与评价闭环

- [ ] 先写测试，要求极小夹具能生成配置、样本ID、训练历史、最佳检查点、validation/calibration指标、运行时和清单。
- [ ] 实现三种学习方法的公平训练、验证损失选模、检查点重载、规则基线与学习方法统一评价。
- [ ] 配置文件明确记录`locked_test_accessed=false`、数据清单哈希、随机种子、批大小、轮数和设备。
- [ ] 完成新增测试、正式模块回归、编译检查和差异检查。

## 任务3：安全同步到RTX 4090

- [ ] 从本地任务分支生成可核验代码包，在`/root/autodl-tmp`创建新的正式训练目录，不修改远端旧仓库。
- [ ] 只打包`airfogsim_formal_tensor_v1`；核对目录不包含`locked-test`或锁定seed，并在远端校验文件哈希。
- [ ] 在远端安装当前项目并运行正式相关测试。

## 任务4：GPU冒烟与正式启动

- [ ] 运行单轮小样本GPU冒烟，核对CUDA利用、有限损失、检查点重载、完整指标与`locked_test_accessed=false`。
- [ ] 根据冒烟显存峰值冻结正式批大小。
- [ ] 启动首次正式非锁定训练，保存PID、日志、配置和输出目录；持续检查直到任务完成或出现明确失败。
- [ ] 汇总结果并更新`本地计划表.md`及PI-JWM主文档。
