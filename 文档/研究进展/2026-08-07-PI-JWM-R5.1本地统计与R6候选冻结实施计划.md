# PI-JWM R5.1本地统计与R6候选冻结实施计划

> **执行要求：** 按`executing-plans`逐项实施；不启用子代理，不创建新worktree，不访问locked-test。

**目标：** 用本地CPU对已验证B/F/G/H/J结果完成独立验收、1/5/20步统计并冻结R6工作候选集合。

**架构：** 新增一个只读分析模块承载纯统计、CPU评价和候选门；薄CLI只负责路径绑定、严格checkpoint加载与产物编排。训练器和模型保持不变。

**技术栈：** Python 3、PyTorch CPU、NumPy、标准库CSV/JSON/hashlib、unittest。

---

## 任务1：本地结果恢复与严格验收

**输入：** `代码/artifacts/remote_handoff/pi_jwm_r5_module_confirmation_complete_20260807.tgz`

- [x] 解包至`代码/artifacts/formal_training/pi_jwm_r5_module_confirmation_v1/`，目标不存在时才执行。
- [x] 运行`python 代码/scripts/verify_r5_module_confirmation_bundle.py <结果目录>`。
- [x] 验证12个新run、3个复用run、12个checkpoint、72个manifest文件及`locked_test_accessed=false`。

## 任务2：TDD实现分析核心

**文件：**

- 新建：`代码/src/pi_jwm/r5_confirmation_analysis.py`
- 新建：`代码/tests/test_r5_confirmation_analysis.py`

- [ ] 先写失败测试：拒绝缺run、重复seed、locked-test、窗口身份漂移和非有限指标。
- [ ] 实现`merge_confirmation_reports()`，只接受B/F/G/H/J×3 seed完整矩阵。
- [ ] 实现`evaluate_model_by_horizon_cpu()`，复用R2尺度与`R4ValidationAccumulator`，不调用CUDA。
- [ ] 实现`analyze_confirmation_reports()`与`analyze_horizon_records()`，复用`MetricSpec`和`paired_comparison()`。
- [ ] 实现`freeze_r6_candidate_set()`，严格执行设计文档中的整体门和专长门。
- [ ] 实现`write_confirmation_analysis_bundle()`，生成JSON/CSV/README和逐文件manifest。
- [ ] 运行`python -m unittest test_r5_confirmation_analysis.py`，要求0失败。

## 任务3：TDD实现CPU分析入口

**文件：**

- 新建：`代码/scripts/analyze_r5_module_confirmation.py`
- 新建：`代码/tests/test_analyze_r5_module_confirmation.py`

- [ ] 先写失败测试：输入不完整、目标目录非空、B manifest不匹配和locked-test请求必须失败。
- [ ] 实现窗口重建与`_window_identity`逐项核对。
- [ ] 用`load_r5_checkpoint`加载B，用`load_confirmation_checkpoint`加载F/G/H/J。
- [ ] 对每个模型分别运行validation horizon 1/5/20，保存45条run-horizon记录。
- [ ] 合并已有validation/calibration聚合报告，写出正式分析bundle。
- [ ] 运行`python -m unittest test_analyze_r5_module_confirmation.py`，要求0失败。

## 任务4：正式CPU运行与候选冻结

**输出：** `代码/artifacts/formal_training/pi_jwm_r5_module_confirmation_analysis_v1/`

- [ ] 运行正式CLI，输入固定dataset、R2 evaluation、原R5 B根目录和R5.1结果根目录。
- [ ] 验证15份聚合报告、45条分horizon记录、配对统计、角色门与manifest。
- [ ] 形成`candidate_freeze.json`，只允许冻结R6角色，不写最终winner。

## 任务5：文档同步与最终验证

**文件：**

- 修改：`本地计划表.md`
- 修改：`D:/禹尧珅/人工智能知识库/北大科研/PIJWM/PIJWM推进.md`
- 新建：`文档/研究进展/2026-08-07-PI-JWM-R5.1本地统计与候选冻结结果.md`

- [ ] 写入真实结果、角色、限制和下一阶段入口。
- [ ] 运行R5.1定向测试及R3—R5相关回归。
- [ ] 运行正式bundle验证、分析manifest复算、Python编译和`git diff --check`。
- [ ] 更新`task_plan.md`、`findings.md`、`progress.md`，记录全部错误和最终状态。
