# PI-JWM 8.12之后推进

> 起始日期：2026-08-12<br>
> 当前文件位置：`文档/知识库/8.12之后推进.md`；2026-08-15已从原外部知识库迁入PI-JWM仓库，后续只维护本文件。<br>
> 本文档记录真实推进、阻塞、实验结果和技术细节；《PIJWM主文档》只保留固定理论与技术规范。<br>
> 状态只按证据填写：未开始 / 核查中 / 接口验证 / 小规模验证 / 正式通过 / 阻塞。不得把计划、候选、接口烟测或历史结果写成最终方法。

## 一、总述与工作顺序

### 1. 总体任务表

| ID | 工作包 | 当前状态（2026-08-13核查） | 前置依赖 | 完成门槛 |
| --- | --- | --- | --- | --- |
| P0 | 理论—PPT—代码—数据—artifact一致性审计 | **首轮正式通过（2026-08-12）**；25项claim已分级并落盘 | 无，最高优先级 | 报告、claim matrix、summary、输入哈希和manifest完整；关键冲突均有处置门 |
| P1 | 信息边字段审计、缺失数据方案与CPU动作边界 | **P1-MVS与P1-A均已冻结（2026-08-13）；5字段E1已进入采集器，13维未伪造** | P0 | 信息边协议及CPU内层规则定义已冻结；不得把5字段实现写成18维完整数据 |
| P2 | 数据采集器、数据集与严格双图重构 | **P2-C仍阻塞（2026-08-15）：P2-B v2 natural-reference为120/120 accepted、0 rejected、0 quarantined，ledger证据链已闭包；仅剩场景/规模/split三门，正式数据集仍未生成** | P1-A | 冻结正式规模/场景/split；只有全部通过后才能生成新数据 |
| P3 | 真实数据辅助 | 未开始 | P1及字段映射 | 确定合法数据源和可对齐子空间；完成纯仿真对照及预训练/微调/外部验证方案 |
| P4 | 世界模型机制修正与小规模重训 | 阻塞于P2 | P2 | 规则递推、动作条件预测、先验/后验或替代理论与代码一致；1/5/20步指标通过门槛 |
| P5 | 模块选择与公平超参数调优 | 未开始 | P4小规模可运行版本 | 给出场景适用理由、搜索空间、相同预算、多种子结果和消融；选定固定模块 |
| P6 | 真实候选Rollout规划器与闭环 | 阻塞；现有实现仅为belief-conditioned direct policy | P4 | 每个候选真实调用世界模型并以预测轨迹成本/风险选动作；执行首动作并反馈重规划 |
| P7 | 扩大训练、指标全集与方法冻结 | 暂不开展 | P2、P4、P5、P6 | 配置/代码/数据/权重/指标可复现；预测、规划、效率、鲁棒性、不确定性齐全 |
| P8 | Baseline复现与公平对比 | 调研可先行，正式复现暂缓 | P1口径和P7评价协议 | 统一数据、动作空间、调参预算与指标；记录必要适配和复现偏差 |
| P9 | 创新证据、论文方法与实验叙事 | 暂不定稿 | P7、P8 | 每项创新有机制差异、对照/消融和限制；文字不超出证据 |
| P10 | 复现、审计与最终测试 | 未开始 | 各阶段持续积累 | manifest、环境、种子、检查点、报告完整；locked test仅在方法冻结后按协议使用 |

### 2. 当前关键不一致与阻塞

| Gap | 已核查事实 | 与理论/旧表述的差异 | 归属 |
| --- | --- | --- | --- |
| G01 | 当前信息边协议预留18槽位，artifact中只有5维出现Mask有效计数，13维为零计数 | 不能把“18维接口存在”写成“18维真实信息已完成”或据此判定R1/R2完整完成 | P1、P2 |
| G02 | 5个有效槽位中，interface_available为硬编码1；CSI实际是有效信道衰减dB；异常路径存在置零后仍有效的风险；rate_sum是跨RB求和而非已分配RB上的实际吞吐 | 名称、物理语义、Mask和评价口径不完全一致 | P1、P2 |
| G03 | 历史rate、served_data、throughput等部分字段可能从现有日志恢复；interference、SINR、outage若保留需重新采集；MCS当前不可得；发射功率/噪声多为固定配置 | 13维不能统一“补齐”；必须按direct、derived、fixed_config、unavailable分别处理 | P1 |
| G04 | R6先由世界模型推断当前belief，再由策略网络对当前候选描述符直接打分；代码路径未逐候选调用model.rollout，也未使用候选相关未来预测 | 这是belief-conditioned direct candidate policy，不是“基于世界模型候选Rollout的规划器” | P0、P6 |
| G05 | R6在AirFogSim中执行所选候选并接收真实反馈，因而有执行反馈闭环 | 只有执行闭环，不等于同时具备候选世界模型规划闭环 | P6 |
| G06 | 旧R6候选包含卸载、RB和CPU分配，而主理论核心动作是卸载与RB | **已处置：** CPU移出核心规划动作，作为每个候选通信后状态上执行的确定性工作守恒内层规则；旧联合CPU结果降级为扩展动作历史证据 | P1-A、P2、P6 |
| G07 | 当前rollout仍依赖历史末帧存在关系/静态结构；理论中的确定性规则更新尚未证明在每一步真实调用并影响后继显式状态 | “规则约束递推”表述超前于实现 | P0、P4 |
| G08 | 现有动作敏感性检查能证明模型输出会随动作变化，但没有配对真实反事实结果 | 不能据此单独宣称因果或反事实预测正确 | P2、P4 |
| G09 | 当前Graph-RSSM确有prior/posterior参数、KL和部署prior rollout；但context prior与posterior都读取同一个当前`base_belief.joint`，未证明严格的“历史先验/当前观测后验”条件分工，也没有latent collapse和概率校准证据 | 只能称轻量RSSM式工作候选；若保留严格RSSM/概率世界模型名称需补条件语义、测试与校准，否则收缩理论名称 | P4 |
| G10 | 现有R3/R5/R6及10k级结果基于旧v3数据协议 | 仅作为历史原型/接口证据；新协议重构后需重训，不能直接外推为最终结果 | P2、P4、P7 |
| G11 | 真实数据、完整baseline、公平调参和指标全集尚未完成 | 暂不能冻结方法或下最终创新结论 | P3、P5、P7、P8、P9 |
| G12 | P2-B绑定的83项AirFogSim依赖已精确恢复：82项来自本机归档直接匹配，`energy_manager.py`由历史两次补丁重放恢复目标混合换行字节；P2-B/P2-C v1与v2验证通过，P2-B v2 natural-reference为120/120 accepted、0 rejected、0 quarantined | source closure和ledger分母冲突已关闭，但这不等于P2-C通过；scenario matrix、正式规模和split仍未冻结 | P2-C |

本轮核查依据：

- `代码/artifacts/datasets/airfogsim_teacher_aligned_v3/tensor_contract.json`列出18个信息边槽位；`normalization_stats.json`的`information_edge_state.count`仅索引0、1、8、11、12非零；
- `代码/src/pi_jwm/airfogsim_teacher_graph_v3.py`将`interface_available`写为1，并按字段是否为`None`设置部分Mask；`代码/scripts/export_dataset_demo.py`异常时将CSI和rate返回0；
- AirFogSim的`getCSI`返回含快衰落的信道衰减矩阵，当前`csi_mean`不是SINR；
- `代码/scripts/run_r6_joint_policy_gpu_training.py::_state_from_online_history`只调用`model.infer_belief`并组装候选描述符；`代码/src/pi_jwm/r6_joint_policy.py::forward`用MLP直接计算候选logit，两个路径均未调用候选`model.rollout`。

### 3. 串行、并行与暂缓

必须串行的主链：

1. P0一致性审计；
2. P1字段取舍与语义冻结；
3. P1-A冻结CPU为候选相关的确定性内层规则；
4. P2采集器、数据和双图重构；
5. P4世界模型小规模验证；
6. P6真实候选Rollout闭环；
7. P5受控调参和模块冻结；
8. P7扩大训练与指标全集；
9. P8正式baseline对比；
10. P9方法与创新定稿；
11. P10最终复现和测试。

可以并行准备：

- P3真实数据源调研、许可和字段映射；
- P8文献、官方代码、环境和最小复现目标盘点；
- 指标实现、manifest规范、测试夹具和实验配置模板；
- 理论适用性论证与候选模型清单，但不能提前定为最终方法。

暂不应开展：

- 基于旧v3协议直接继续100k或更长训练；
- 为了满足18维接口而用常量、随机量或无法验证的代理量补齐13维；
- 在一致性门前冻结模型、规划器、动作空间或最终创新表述；
- 访问locked test；
- 用未经公平调参的单次分数淘汰模块或baseline。

### 4. 最近一个最小可验证步骤

**步骤P2-C：正式数据规模与分布方案审计（P2全双图非训练采集器预检于2026-08-14已通过）。**

- 冻结自然轨迹目标规模、场景/节点/无线与wired边类型、任务生命周期、DAG深度、多流、RB复用、失败和Mask有效率的覆盖门；fixture覆盖必须与自然分布覆盖分开报告；
- 冻结train/validation/test seed划分、重放要求、quarantine与拒绝率上限、manifest/source closure和正式数据发布目录；
- 继续逐字段报告5字段E1的`valid_mask/missing_reason`分布，不增加13维占位，不把初始CSI不可用填成0；
- 生成数据前复核CPU规则、DAG dependency gate和AirFogSim lifecycle-alias guard在正式配置中仍生效；
- 不访问locked test、不启动GPU、不复用旧v3轨迹冒充v4正式数据。

验收标准：形成机器可读的规模、覆盖、split、拒绝与字段有效率配置及审计报告；该步骤通过后才批准正式v4数据生成，仍不等于数据集完成或可训练。

## 二、待办清单

- [x] P0 建立并完成理论—PPT—代码—数据—artifact首轮逐项映射
- [x] P1 完成18槽位真值表、字段取舍和最小可验证导出
- [x] P1-A 冻结卸载+RB核心动作与候选相关CPU内层规则
- [x] P2-A 实现并验证CPU内层规则纯函数、AirFogSim callback接口和非训练预检bundle
- [x] P2-B 实现并验证v4全双图非训练采集器、三相位frame、联合动作、replay、fixture和manifest
- [ ] P2-C **首轮审计已完成但阻塞**：先恢复83项AirFogSim依赖闭包，再冻结并执行正式规模/分布/split审计；尚未批准生成新v4数据
- [ ] P3 确定真实数据的可对齐子空间与辅助方式
- [ ] P4 修正并验证世界模型核心机制
- [ ] P5 完成候选模块理由、消融和公平超参数搜索
- [ ] P6 实现并测试真正的候选世界模型Rollout规划闭环
- [ ] P7 扩大训练并输出完整指标，满足门槛后冻结方法
- [ ] P8 复现并公平比较baseline
- [ ] P9 固化创新证据、方法边界和论文叙事
- [ ] P10 完成复现包与最终审计

## 三、分项工作与真实技术细节

> 以下各节从2026-08-12起按真实发生顺序填写；当前先保留空节，不预写尚未发生的实现或结果。

### P0 理论—实现—证据一致性审计

**完成日期：** 2026-08-12<br>
**正式产物：** `代码/artifacts/audit/pi_jwm_p0_consistency_audit_v1/`

- `REPORT.md`：首轮独立核查报告，含项目现状、冲突、风险、工作依赖、已有实验有效性和P1-MVS验收门；
- `claim_matrix.csv`：25项理论/实现/证据主张逐项标记`verified/partial/contradicted/not_implemented/not_verifiable`；
- `audit_summary.json`：机器可读数量、阻塞门、下一步和测试状态；
- `input_hashes.csv`与`manifest.json`：权威输入绑定及审计输出哈希。

本轮直接复算与验证：

- 54条非锁定张量的信息边Mask维度为18，仅索引0/1/8/11/12有效，13维始终无有效观测；
- R1顶层278文件manifest逐项SHA-256重算0不匹配；
- R2注册43项指标，但只有22项事实映射，结果行仍有`not_computable/not_applicable`；
- R5只冻结R6工作候选，`final_method_frozen=false`；
- R6 18/18个10k run完成，但`formal_budget_complete=false`且当前路径没有逐候选`model.rollout`；
- 主文档“卸载+RB、CPU固定”与R6联合控制CPU构成明确动作空间冲突；
- 理论`U^det`逐步规则层在R3/R4 rollout主干中未实现；
- 106项定向单元测试通过。测试通过仅证明既定接口/条件，不替代理论机制证据。

P0期间未启动长GPU、未读取locked-test轨迹/标签、未清理脏工作树。P0首轮完成不解除P2/P4/P6/P7阻塞；P1-MVS、P1-A和P2-A已于2026-08-13依次完成协议审计、设计冻结和CPU规则/接口预检，当前下一步为P2采集器契约与单步非训练集成。

### P1 信息边字段与缺失数据

**2026-08-13实施状态：** v4信息边设计已确认，P1-MVS已正式通过。设计与计划分别为`文档/研究进展/2026-08-12-PI-JWM-v4信息边均衡协议设计.md`和`文档/研究进展/2026-08-13-PI-JWM-P1-MVS实施计划.md`；正式机器产物位于`代码/artifacts/audit/pi_jwm_p1_information_edge_contract_v4/`。

- 旧18槽不再机械补齐。`interface_available`移出连续字段；旧`csi_mean`收缩为动作前信道衰减统计；旧`rate_sum`因全RB快照与已分配RB事件语义不一致，不直接迁移。
- v4分为E0结构/动作、E1五个链路级核心状态、E2上一时隙逐RB无线增强、E3当前动作前逐RB信道上界诊断。默认候选为E1，E2/E3是否保留必须由非劣、效率和缺失鲁棒门决定。
- 权威RB动作采用稀疏`(time,flow,information_edge,RB)` COO，不能只保存RB数量或单跳edge索引；卸载动作单独保存。P1-A已冻结CPU不进入核心动作，而是在每个候选通信后状态上执行同一确定性工作守恒规则。
- AirFogSim rate与task size的bit/byte单位换算尚未证实，P2校准前只使用`AirFogSim data-unit/s`与`AirFogSim data-unit`，不冒称Mbps或MB。
- 字段来源、有效Mask和缺失原因分别保存；未激活RB的占位零、钳位SINR或零rate不自动视为合法真实观测。
- registry逐字段声明适用边类型：无线专属量在wired边为`not_applicable`；理论适用但当前没采到必须标`source_absent/not_collected`，不能用“不适用”隐藏缺口。
- 字段价值实验采用同一超集模型仅改变Mask；紧凑E1/E2部署效率另行比较，避免把模型容量差异误写成信息价值。
- P1-MVS只允许生成registry、旧槽迁移表、非锁定极小样例、coverage、拒绝清单和manifest；不重建正式数据、不训练、不访问locked test。

本轮实际验收：

- registry共29项，其中E1核心字段5项；来源等级严格为`direct/derived/fixed_config/unavailable`，缺失原因9种，旧18槽迁移18行。
- 54条非锁定轨迹按真实present边、split、6类场景和9种无线边类型复算，旧有效索引仍为0/1/8/11/12，每维计数5,854,752；13维为0。当前没有wired真实样例。
- 270条旧观测与5条协议fixture全部标记`training_eligible=false`和`v4_field_implemented=false`；旧字段名与候选v4目标分开，未伪造v4训练数据。
- 独立代码审查发现首版验收门在18槽顺序/schema、失败证据留存、代码哈希、数值/守恒/dtype和locked端到端I/O证据上不完整；均先补失败测试再修复，审查前产物已移入带`pre_independent_review_fix_20260813`后缀的目录，不作为正式证据。
- 修复后在同一54条非锁定轨迹上重跑，5/13结论不变；54项v3+v4相关回归通过，6个输出、110个输入和2个代码文件hash独立复算0失败，协议/配置哈希一致；locked输入路径0，GPU未启动。
- `v4_collector_implemented=false`、`v4_dataset_complete=false`、`v4_model_trained=false`。P2-A已完成CPU规则和callback接口预检；P2正式数据生成尚未开始。

### P1-A CPU动作边界冻结

**2026-08-13状态：已由用户确认并冻结。** 正式设计位于`文档/研究进展/2026-08-13-PI-JWM-P1-A-CPU动作边界冻结设计.md`。

- 核心动作固定为卸载与RB；CPU不进入候选动作、策略输出或候选组合空间。
- CPU不是与候选无关的常量。每个候选在完成卸载、RB及通信更新后，依据该候选实际形成的可计算任务集合调用`PIJWM-CPU-Inner-Rule-v1`，随后更新计算工作、任务阶段、队列和能耗。
- CPU规则固定为无可调权重的封顶均分工作守恒规则；不读取未来、deadline、priority、locked身份或策略输出。
- 旧v3 60条轨迹混用了三种CPU策略，不能作为单一冻结规则下的正式新数据；R1结构/manifest工程证据保留，P2需新目录重建数据。
- R6 CPU配对保留为CPU敏感性证据；旧联合CPU与10k结果保留为扩展动作/工程历史证据，不再具备继续100k的资格。
- P1-A自身只冻结设计；随后P2-A已实现CPU规则代码和callback接口预检，但仍没有P2采集器、v4数据、完整AirFogSim轨迹证据或候选世界模型rollout，因此没有解除P4/P6/P7阻塞。

### P2 数据集与严格双图重构

#### P2-A CPU内层规则最小可执行验证

**完成日期：** 2026-08-13<br>
**正式产物：** `代码/artifacts/preflight/pi_jwm_cpu_inner_rule_v1/`

- `cpu_inner_rule_v1.py`实现逐节点封顶均分：`d_m=remaining_work_m/slot_seconds`，`f_m=min(d_m,lambda_i)`，总分配为节点容量与总需求二者较小值；输出按节点和任务ID稳定排序。
- 空任务、零容量、需求不足、需求过载、非等需求、多节点、浮点守恒和候选后通信任务集合变化均已测试；负值、非有限值、重复任务、缺容量和任务—节点关系冲突均拒绝。
- `airfogsim_cpu_inner_rule_v1.py`适配`TaskManager.computeTasks`的真实callback输入/输出形状，读取真实`Task`方法与节点`getFogProfile()['cpu']`。同一输入下适配结果与纯函数完全一致。
- 当前Python环境直接导入AirFogSim顶层会先遇到GBK控制台emoji编码错误，改UTF-8后仍缺可视化依赖`osmnx`。验证没有修改第三方源码，而是显式执行仓库真实`enum_const.py`、`mission.py`和`task.py`源码并绕开顶层GUI副作用。因此该项是AirFogSim真实Task源码的callback接口验证，不是完整AirFogSim轨迹运行。
- 正式bundle包含`rule_contract.json`、`sample_cases.csv`、`rejected_records.csv`、`summary.json`和`manifest.json`。8个合约案例形成14条通过记录，另有6条预期拒绝记录；全部为`contract_fixture`、`training_eligible=false`。
- 聚合20项测试与Python编译通过。机器状态明确为`full_airfogsim_trajectory_executed=false`、`v4_collector_implemented=false`、`v4_dataset_complete=false`、`candidate_rollout_planner_complete=false`、`locked_test_accessed=false`、`gpu_started=false`。

下一串行门不是直接生成正式数据，而是先冻结并实现P2采集器逐步契约：候选通信更新、CPU规则调用、任务/队列/能耗更新、v4字段与Mask/MissingReason、动作COO和拒绝记录必须在一个单步非训练集成样例中逐项对齐。通过该门后才批准在新目录生成正式v4数据。

#### P2-B v4全双图非训练采集器canonical preflight

**完成日期：** 2026-08-14<br>
**仓库分支：** `codex/v4-full-dual-graph-collector-design`<br>
**实现提交：** `f792287`（完整实现链从`bfae19a`到`f792287`）<br>
**机器证据：** `代码/artifacts/preflight/pi_jwm_p2_full_dual_graph_collector_v1/`（当前隔离worktree本地artifact，未提交为源码）

已证实事实：

- seeds `0/1/2`分别运行`orthogonal`和`interference_reuse`两臂，共6条自然轨迹、每条20个真实AirFogSim step、总计120 frame；每条reference使用第二个全新环境重放，6/6通过且exact mismatch为0。
- 十项边界fixture全部通过：多任务多流、跨发射端RB复用、wired、local、多跳卸载、多跳回传、节点消失—重现、路由中断、deadline失败和TTI失败。它们均为真实AirFogSim对象与真实step，但含受控初态或瞬态干预，`fixture=true`、`training_eligible=false`，不能写成自然数据覆盖。
- E1按每条物理边只输出5个真实命名字段：当前信道衰减均值/标准差和上一帧active flow数、有效rate、served data；每字段独立Mask和MissingReason。旧18槽的13个缺失维没有补零、常量或代理量。
- 初始时刻channel index尚未建立时，结构边仍保留，CSI显式标`NODE_INDEX_UNAVAILABLE_AT_DECISION`。DAG前驱未完成/已失败的waiting任务分别记录`dependency_not_satisfied/dependency_failed`，不调用setter。
- 独立源码核查发现AirFogSim的`getOffloadingTasksWithNumber()`会因list别名把returning任务写入offloading集合。PI-JWM适配层只在真实`env.step()`边界使用复制查询视图并随后恢复，第三方源码未修改。
- v4及历史P1/P2 focused regression在P2-B生成/合并时为`150/150`通过。Task 10自审发现并修正首版manifest只绑定8个新增源码、以及AirFogSim junction经`resolve()`后产生临时`.worktrees/...`键两项问题；bundle仍绑定7个artifact与116个运行源码、测试、设计和配置依赖。P2-C于2026-08-14重新核验时，7个artifact哈希0不匹配、绝对路径/worktree前缀键0，但外部AirFogSim checkout/junction已不在当前工作区，导致80个Python源码和3个配置缺失，完整source closure当前无法通过。两版修复前bundle只保留为诊断归档，不再作为canonical证据；当前bundle也不得据此升级为正式数据生成授权。

严格状态边界：只允许`v4_collector_implemented=true`。正式数据集、训练资格、模型训练、GPU、locked test、候选世界模型rollout规划器和最终方法全部仍为false。下一步必须先完成P2-C正式数据规模与分布审计，不能直接继续旧100k训练。

#### P2-C 正式数据规模与分布首轮审计

**审计日期：** 2026-08-14<br>
**当前状态：** `audit_completed_blocked`<br>
**机器证据：** `代码/artifacts/audit/pi_jwm_p2c_scale_distribution_audit_v1/`<br>
**详细报告：** `文档/研究进展/2026-08-14-PI-JWM-P2-C正式数据规模与分布审计.md`

- 独立复算自然数据为6条episode、120帧；10项fixture只在coverage报告中，不进入自然帧统计。6/6 fresh-environment replay通过。
- 自然帧覆盖`V/U/I/C`节点、九类无线有向边、DAG深度`0/4/5/6`、22个多flow帧、9个跨发射端RB复用帧、25条outage传输和failed生命周期；自然wired hop为0，wired只有fixture证据。
- E1共5760条边记录，row width恒为5。CSI两字段各`4392 valid / 1368 CURRENT_CSI_UNAVAILABLE`；三个历史字段各`5472 valid / 288 NO_HISTORY`；旧13维占位为0。
- 120/120帧绑定`PIJWM-CPU-Inner-Rule-v1`，26条DAG依赖未满足decision被明确挡住，120/120帧记录lifecycle-alias guard。
- 新发现完整source closure当前失败：required/artifact缺失为0、artifact哈希不匹配为0，但80个AirFogSim Python源文件和3个配置当前缺失。bundle数值统计未被改写，但正式采集与可复现性声明被阻断。
- P2-C新增8项审计/发布/篡改检测测试全部通过；P1/P2 focused suite现跑151项、16项因同一AirFogSim目录/源码缺失根因报`FileNotFoundError`，因此不能沿用旧的“当前150/150通过”表述。
- 其余阻断为：正式scenario matrix、目标规模和seed split未冻结；canonical bundle没有完整attempt/reject分母，不能把0个quarantine推断为拒绝率0。
- 候选配置明确`formal_data_approved=false`、`training_eligible=false`，没有拍脑袋填写轨迹数或split。下一最小步骤是恢复生成P2-B时的原始external checkout，并要求83项missing/mismatch均为0；通过前不生成正式轨迹、不启动GPU、不访问locked test。

#### P2-C v2 ledger证据闭包

**闭包日期：** 2026-08-15<br>
**当前状态：** `audit_completed_blocked`<br>
**机器证据：** `代码/artifacts/audit/pi_jwm_p2c_scale_distribution_audit_v2_pre_document_closure_20260814/`

- P2-B v2 natural-reference ledger记录120 attempts = 120 accepted + 0 rejected，quarantined=0；拒绝率分母门已关闭。
- P2-C v2审计JSON与candidate config已与预文档候选逐字节一致；final candidate仅在本地忽略目录生成，manifest只更新证据文档source hash。
- P2-B/P2-C v1与v2 verifier均为`passed=true`且错误列表为空；该闭包只证明ledger和已绑定证据一致，不批准正式数据生成。
- 当前只剩`scenario_matrix_not_frozen`、`formal_scale_not_frozen`、`formal_split_not_frozen`三个阻断；`formal_data_approved=false`、`training_eligible=false`、`gpu_started=false`、`locked_test_accessed=false`保持不变。

### P3 真实数据辅助

### P4 世界模型修正与训练

### P5 模块选择、消融与超参数

### P6 候选Rollout规划器与真实闭环

### P7 扩大训练、指标与方法冻结

### P8 Baseline复现与对比

### P9 创新证据与论文方法

### P10 复现、审计与最终测试
