# PI-JWM R2 Evaluation Protocol Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在`PIJWM-DG-Contract-v3`上冻结可运行、可审计且不访问locked-test的统一评价与公平实验协议。

**Architecture:** `evaluation_protocol_v3.py`保存指标和实验规则；`teacher_evaluation_v3.py`流式计算v3简单基线并汇总真实系统sidecar；构建脚本物化注册表、结果、模板、验证和manifest。旧指标公式通过适配复用，不修改旧语义历史模块。

**Tech Stack:** Python 3、NumPy、标准库JSON/CSV/hashlib、unittest、R1正式v3张量与AirFogSim正式指标sidecar。

---

### Task 1: 冻结指标与公平实验协议

**Files:**
- Create: `代码/src/pi_jwm/evaluation_protocol_v3.py`
- Test: `代码/tests/test_evaluation_protocol_v3.py`

- [x] 先写失败测试，要求注册表覆盖状态、事件、系统、安全、不确定性和运行时层，并为每项提供公式、单位、分子、分母、mask、source、方向和N/A规则。
- [x] 运行`python -m unittest test_evaluation_protocol_v3.py`，确认因模块或接口不存在而失败。
- [x] 实现注册表、公平配置和验证器；拒绝重复ID、locked-test拟合、calibration选型和不等预算。
- [x] 重新运行测试并确认通过。

### Task 2: 实现v3基线评价适配器

**Files:**
- Create: `代码/src/pi_jwm/teacher_evaluation_v3.py`
- Test: `代码/tests/test_teacher_evaluation_v3.py`

- [x] 先写失败测试，固定信息边活动/速率、feature mask、zero/last、任务生命周期、逐seed宏平均和明确N/A行为。
- [x] 运行测试，确认因适配器不存在而失败。
- [x] 实现流式累加器、zero/last预测和v3轨迹评价；不读取locked-test，不把信道字段放回物理边。
- [x] 运行测试并确认通过。

### Task 3: 物化正式R2产物

**Files:**
- Create: `代码/scripts/build_teacher_evaluation_protocol_v3.py`
- Test: `代码/tests/test_build_teacher_evaluation_protocol_v3.py`
- Output: `代码/artifacts/evaluation/pi_jwm_eval_protocol_v3/`

- [x] 先写三轨迹fixture失败测试，验证只处理train/validation、读取真实指标sidecar、输出报告模板并生成完整manifest。
- [x] 实现构建器和CLI，读取R1 summary/contract/index、逐seed评价zero/last、汇总22项真实系统指标可用性。
- [x] 运行fixture测试并确认通过。
- [x] 在54条非锁定正式轨迹上执行构建，不访问6条locked-test标签。

### Task 4: 全量验收与文档同步

**Files:**
- Modify: `D:/禹尧珅/人工智能知识库/北大科研/PIJWM/PIJWM推进.md`
- Modify: `本地计划表.md`
- Modify: `progress.md`
- Modify: `findings.md`

- [x] 独立复算R2 manifest，核对54/6 split、指标完整性、基线有限值/N/A和系统sidecar状态。
- [x] 运行R2专项测试、R1回归测试、旧正式指标测试、源码编译和`git diff --check`。
- [x] 更新R2状态、正式产物、预算、指标边界和R3进入条件。
- [x] 回读机器报告，只有所有验收门通过后才声明R2完成。

## 2026-08-03 返修前审计补充（已完成）

- [x] 将22个AirFogSim事实指标逐项映射到canonical metric ID，并生成统一`evaluation_rows.json/csv`；不再把事实sidecar与预测基线分成无法连接的两套口径。
- [x] 将历史名`physical_link_active_ratio`降级为来源兼容名，正式`system.information_link_active_ratio`从v3信息边的`outcome.active_task_count`、`present`和`feature_mask`重新计算；54条轨迹与旧值最大绝对差为0，但语义来源已纠正。
- [x] zero连续基线改为train-only特征均值；last-persistence仅在前后时刻均有效时计分；任务生命周期固定五类；单轨迹样本标准差不再伪报0。
- [x] 注册并实现`selection.required_continuous.normalized_error`，冻结十个连续分量、train-only尺度、四项各0.25权重和calibration阈值规则。
- [x] 输入文件先按上游manifest验证SHA-256，再读取；新增代码/runtime/git/input provenance；54条非锁定和6条锁定轨迹身份集合均与上游摘要一致。
- [x] 正式候选包验证通过：43项注册指标、22项事实指标、108份zero/last报告、3348条canonical rows、13个manifest受管文件；锁定测试仍未读取。
