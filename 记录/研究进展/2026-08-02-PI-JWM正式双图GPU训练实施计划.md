# PI-JWM正式双图GPU训练实施计划

> **执行要求：** 按TDD顺序完成；只同步非锁定张量；远端使用独立目录，不覆盖历史实验。

## 任务1：GPU协议与设备搬运

- [x] 新建`代码/tests/test_run_formal_dual_graph_gpu_train_v1.py`，先验证CUDA设备约束、split白名单、递归张量搬运和锁定测试拒绝。
- [x] 运行测试并确认因GPU入口不存在而失败。
- [x] 新建`代码/scripts/run_formal_dual_graph_gpu_train_v1.py`，实现最小协议检查和递归设备搬运。
- [x] 运行新增测试及45项正式CPU回归测试。

## 任务2：GPU训练与评价闭环

- [x] 先写测试，要求极小夹具能生成配置、样本ID、训练历史、最佳检查点、validation/calibration指标、运行时和清单。
- [x] 实现三种学习方法的公平训练、验证损失选模、检查点重载、规则基线与学习方法统一评价。
- [x] 配置文件明确记录`locked_test_accessed=false`、数据清单哈希、随机种子、批大小、轮数和设备。
- [x] 完成新增测试、正式模块回归、编译检查和差异检查。

## 任务3：安全同步到RTX 4090

- [x] 从本地任务分支生成可核验代码包，在`/root/autodl-tmp`创建新的正式训练目录，不修改远端旧仓库。
- [x] 只打包`airfogsim_formal_tensor_v1`；核对目录不包含`locked-test`或锁定seed，并在远端校验文件哈希。
- [x] 在远端安装当前项目并运行正式相关测试。

## 任务4：GPU冒烟与正式启动

- [x] 运行单轮小样本GPU冒烟，核对CUDA利用、有限损失、检查点重载、完整指标与`locked_test_accessed=false`。
- [x] 根据冒烟显存峰值冻结正式批大小为32。
- [x] 完成首次正式非锁定训练，保存日志、配置、训练历史、最佳检查点、完整指标和SHA-256清单。
- [x] 下载结果并在本地逐项复算清单；汇总结果并更新`本地计划表.md`及PI-JWM主文档。

## 完成状态

- 完成时间：2026-08-02。
- 正式运行：seed 20260802，hidden=64，batch=32，20轮，完整使用10440/3480/1740个train/validation/calibration窗口。
- 结果目录：`代码/artifacts/formal_training/formal_seed20260802_h64_b32_e20_39e359a/`。
- 结论边界：训练与评价闭环已经完成；locked-test未访问；耦合模型的链路活动预测明显改善，但连续状态与任务时序尚未全面超过末值保持，因此`formal_performance_claim_ready=false`。
