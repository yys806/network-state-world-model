# PI-JWM R6 CPU预检实施计划

| 阶段 | 实施内容 | 预期产物 | 资源 | 状态 |
|---|---|---|---|---|
| R6.0 角色和输入锁定 | 读取R5.1 `candidate_freeze.json`，校验B/G/J/F角色、数据集manifest和split边界 | 输入绑定和角色审计 | CPU | 待执行 |
| R6.1 台账与硬约束审计 | 扫描54条非锁定轨迹的graph/resource/metric报告，检查动作记录、CPU容量、流守恒、能量方程和依赖语义 | 轨迹级安全审计CSV/JSON | CPU | 待执行 |
| R6.2 CPU闭环下界统计 | 按CPU策略和split汇总完成率、P95/P99时延、吞吐量、能耗、利用率、公平性等真实指标 | 策略级真实指标表 | CPU | 待执行 |
| R6.3 独立验收 | 运行定向测试、manifest复算、文档检查，写明go/no-go和不能外推的边界 | R6 CPU预检bundle和结果说明 | CPU | 待执行 |

本阶段不重新训练、不访问locked-test、不启动GPU；失败记录保留在bundle中，禁止覆盖既有R5产物。
