# P4 Link 事件概率校准边界实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use `subagent-driven-development` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在不修改模型、weighted BCE、数据、方案 A、raw threshold candidates 或性能门的前提下，把 link activity 的 raw weighted score 转换为 calibration-only 拟合的正式事件概率，并输出可审计的 NLL、Brier、ECE 与决策等价证据。

**Architecture:** `formal_binary_calibration_v1.py` 是唯一概率数学边界；`formal_world_model_metrics_v1.py` 只消费已经定义好的 link 事件概率；正式 runner 在 calibration split 拟合一个 scalar temperature、先按 legacy raw 坐标选候选身份，再映射到概率阈值；独立 CPU audit 对当前 sentinel checkpoint 做只读验收。所有分类决策必须与 legacy raw-score 完全相同，因此本实施不会改变当前 No-Go。

**Tech Stack:** Python 3、PyTorch、NumPy、`unittest`、JSON/CSV、SHA-256、PowerShell。

---

## 0. 执行边界

- 当前阶段：P4 blocked。
- 允许设备：CPU only；只允许单元测试、fixture micro-test 和当前 checkpoint 只读 inference。
- 禁止：GPU、CPU 大训练、follow-up seeds、threshold sweep、loss/model/tensor 修改、P6、planner、`locked_test`。
- 冻结当前结果：candidate/persistence validation link-F1=`0.3724/0.5901`，sentinel=`no_go`。
- 当前工作树包含大量用户未提交变化。每个任务使用 `git diff --check` 和路径级 diff 作为 checkpoint；未经用户单独授权不创建 commit，避免把不相关变化混入提交。

## 1. 文件职责

| 文件 | 操作 | 唯一职责 |
| --- | --- | --- |
| `code/src/pi_jwm/formal_binary_calibration_v1.py` | Modify | 解析反演、单温度拟合、raw threshold 映射、NLL/Brier/ECE |
| `code/tests/test_formal_binary_calibration_v1.py` | Modify | 概率数学、拟合、分箱、错误输入的 TDD 证据 |
| `code/src/pi_jwm/formal_world_model_metrics_v1.py` | Modify | 让 link metric 使用正式事件概率并输出逐 horizon/overall 概率指标 |
| `code/tests/test_formal_world_model_metrics_v1.py` | Modify | metric accumulator 的概率语义与 mask 回归 |
| `code/scripts/run_formal_dual_graph_gpu_train_v1.py` | Modify | calibration-only 拟合、raw candidate 选择、mapped threshold 与 sidecar 产物 |
| `code/tests/test_run_formal_dual_graph_gpu_train_v1.py` | Modify | 正式 runner 的 split、artifact、decision-equivalence 回归 |
| `code/scripts/run_formal_p4_link_probability_calibration_v1.py` | Create | 对冻结 sentinel checkpoint 执行 CPU-only 校准审计 |
| `code/tests/test_run_formal_p4_link_probability_calibration_v1.py` | Create | 审计 schema、provenance、停止门和 No-Go 保护 |
| `code/artifacts/audit/pi_jwm_p4_link_probability_calibration_20260831/` | Generate | 当前 sentinel 的新机器证据，不覆盖历史结果 |
| `task_plan.md`、`progress.md`、`findings.md` | Modify | 过程记录 |
| `记录/本地计划表.md`、`记录/PIJWM主文档.md`、`记录/8.12之后推进.md`、`PROJECT_CONTEXT.md` | Modify after acceptance | 只记录已验证边界，不提前写性能通过 |

## Task 1：冻结概率数学边界

**Files:**

- Modify: `code/src/pi_jwm/formal_binary_calibration_v1.py`
- Modify: `code/tests/test_formal_binary_calibration_v1.py`

- [x] **Step 1：写解析反演和阈值映射失败测试**

在 `FormalBinaryCalibrationV1Tests` 中加入：

```python
def test_inverse_temperature_probability_and_threshold_mapping_are_equivalent(self):
    from pi_jwm.formal_binary_calibration_v1 import InverseTemperatureCalibration

    calibration = InverseTemperatureCalibration(pos_weight=50.0, log_temperature=0.0)
    raw_logits = torch.tensor([-4.0, 0.0, 2.0, 4.0], dtype=torch.float64)
    raw_scores = torch.sigmoid(raw_logits)
    probabilities = calibration.probabilities_from_weighted_logits(raw_logits)
    expected = torch.sigmoid(raw_logits - math.log(50.0))
    torch.testing.assert_close(probabilities, expected, atol=1e-12, rtol=0.0)

    for raw_threshold in (0.1, 0.3, 0.5, 0.7, 0.9):
        mapped = calibration.map_raw_threshold(raw_threshold)
        self.assertTrue(torch.equal(raw_scores >= raw_threshold, probabilities >= mapped))
```

再加入错误边界：`pos_weight<=0`、非有限 logit、raw threshold 不在 `(0,1)` 必须 `ValueError`。

- [x] **Step 2：运行 RED**

Run:

```powershell
$env:PYTHONPATH='D:\shen\PKU\PIJWM\code\src'
python -m unittest discover -s .\code\tests -p 'test_formal_binary_calibration_v1.py' -v
```

Expected：新测试因 `InverseTemperatureCalibration` 尚不存在而失败；原反演测试继续通过。必须保存准确失败文本到 `progress.md`，不能把 import/拼写错误当作 RED。

- [x] **Step 3：写最小数学对象**

在 `formal_binary_calibration_v1.py` 加入以下接口，字段名不得改动：

```python
@dataclass(frozen=True)
class InverseTemperatureCalibration:
    pos_weight: float
    log_temperature: float

    @property
    def temperature(self) -> float:
        return 1.0 if abs(float(self.log_temperature)) <= 1e-12 else math.exp(float(self.log_temperature))

    def unweighted_logits(self, weighted_logits: torch.Tensor) -> torch.Tensor:
        return weighted_logits - math.log(self.pos_weight)

    def probabilities_from_weighted_logits(self, weighted_logits: torch.Tensor) -> torch.Tensor:
        return torch.sigmoid(self.unweighted_logits(weighted_logits) / self.temperature)

    def map_raw_threshold(self, raw_threshold: float) -> float:
        raw_logit = math.log(raw_threshold / (1.0 - raw_threshold))
        return float(torch.sigmoid(torch.tensor((raw_logit - math.log(self.pos_weight)) / self.temperature, dtype=torch.float64)))
```

`__post_init__` 必须验证 `pos_weight>0`、两字段有限；两个 tensor 方法必须拒绝非有限输入。

- [x] **Step 4：写拟合和指标失败测试**

加入三个测试：

```python
def test_fit_inverse_temperature_uses_unweighted_full_mean_nll(self):
    weighted_logits = torch.tensor([-1.0, 0.5, 2.0, 4.0], dtype=torch.float64)
    labels = torch.tensor([0.0, 0.0, 1.0, 1.0], dtype=torch.float64)
    calibration, report = fit_inverse_temperature(weighted_logits, labels, pos_weight=50.0)
    self.assertEqual("calibration", report["fit_split"])
    self.assertEqual("mean_unweighted_bce_with_logits", report["objective"])
    self.assertLessEqual(report["fitted_nll"], report["identity_nll"] + 1e-12)
    self.assertGreater(calibration.temperature, 0.0)

def test_binary_probability_metrics_use_fixed_fifteen_bins(self):
    probabilities = torch.tensor([0.0, 1.0 / 15.0, 14.0 / 15.0, 1.0], dtype=torch.float64)
    labels = torch.tensor([0.0, 0.0, 1.0, 1.0], dtype=torch.float64)
    report = binary_probability_metrics(probabilities, labels)
    self.assertEqual(15, report["ece_bin_count"])
    self.assertEqual([[0.0, 1.0 / 15.0], [1.0 / 15.0, 2.0 / 15.0]], report["ece_bins"][:2])
    self.assertEqual([14.0 / 15.0, 1.0], report["ece_bins"][-1])

def test_choose_legacy_threshold_keeps_raw_tie_break_and_decisions(self):
    calibration = InverseTemperatureCalibration(pos_weight=50.0, log_temperature=0.0)
    raw_scores = torch.tensor([0.8, 0.2], dtype=torch.float64)
    labels = torch.tensor([True, False])
    selection = choose_legacy_threshold(raw_scores, labels, calibration)
    self.assertEqual(0.5, selection["selected"]["raw_threshold"])
    self.assertTrue(selection["decision_equivalence_to_legacy"])
```

该数据使 raw `0.3/0.5/0.7` 的 F1 同为 1，正确选择必须是 raw `0.5`；映射到 `pos_weight=50` 的概率坐标后不得重新判定距 0.5 的远近。测试文件同时新增 `import math`，并显式导入本段使用的三个函数和 calibration class。

- [x] **Step 5：再次运行 RED，然后实现拟合/指标/选择**

实现固定接口：

```python
fit_inverse_temperature(weighted_logits, labels, *, pos_weight, fit_split="calibration")
binary_probability_metrics(probabilities, labels, *, ece_bin_count=15)
choose_legacy_threshold(raw_scores, labels, calibration, raw_thresholds=(0.1, 0.3, 0.5, 0.7, 0.9))
```

拟合必须使用 CPU float64、全量向量、`q=log(T)`、LBFGS 参数与设计规格一致；loss 固定为：

```python
F.binary_cross_entropy_with_logits(
    (weighted_logits.double() - math.log(pos_weight)) / torch.exp(q),
    labels.double(),
    reduction="mean",
)
```

ECE 的区间固定为前 14 个左闭右开、最后一个 `[14/15,1]`。选择函数必须先按 `(f1, -abs(raw_threshold-0.5))` 选择 raw identity，再映射阈值，并逐候选断言 decisions 等价。

- [x] **Step 6：运行 GREEN 和静态检查**

Run:

```powershell
python -m unittest discover -s .\code\tests -p 'test_formal_binary_calibration_v1.py' -v
python -m compileall -q .\code\src\pi_jwm\formal_binary_calibration_v1.py .\code\tests\test_formal_binary_calibration_v1.py
git diff --check -- .\code\src\pi_jwm\formal_binary_calibration_v1.py .\code\tests\test_formal_binary_calibration_v1.py
```

Expected：全部通过；没有 warning、NaN 或非有限 JSON 字段。

## Task 2：让 metric accumulator 使用正式 link 概率

**Files:**

- Modify: `code/src/pi_jwm/formal_world_model_metrics_v1.py`
- Modify: `code/tests/test_formal_world_model_metrics_v1.py`

- [x] **Step 1：写 metric RED 测试**

新增测试构造 `raw_logit=log(50)+logit(p)`、`T=1`，并传入 mapped threshold：

```python
calibration = InverseTemperatureCalibration(pos_weight=50.0, log_temperature=0.0)
accumulator = FormalMetricAccumulator(
    identity_stats(),
    thresholds={"link_activity": calibration.map_raw_threshold(0.9)},
    link_probability_calibration=calibration,
)
```

断言 overall 与每个 horizon 都存在：

```text
event.link_activity.nll
event.link_activity.brier
event.link_activity.ece
```

并断言现有 F1 与直接使用 raw score threshold `0.9` 的 legacy accumulator 完全相同；registry 包含三个概率指标。

- [x] **Step 2：运行 RED**

Run:

```powershell
python -m unittest discover -s .\code\tests -p 'test_formal_world_model_metrics_v1.py' -v
```

Expected：因 constructor 不接受 `link_probability_calibration` 或概率指标不存在而失败；既有测试继续通过。

- [x] **Step 3：最小修改 accumulator**

- `FormalMetricAccumulator.__init__` 新增 `link_probability_calibration: InverseTemperatureCalibration | None = None`。
- `_MetricBucket.add_binary` 新增 keyword-only `probabilities: torch.Tensor | None = None`；为 `None` 时继续 `sigmoid(logits)`，只对 link 传正式概率。
- `metric_registry()` 仅新增 link 的 `nll/brier/ece`，不扩展其他事件。
- `_finalize_bucket` 只用 bucket 中已经保存的正式 link probabilities 与 labels 调用 `binary_probability_metrics`。
- `finalize()` 新增 `link_probability_calibration` metadata；不得把 raw score 称为 probability。

- [x] **Step 4：运行 GREEN 与全 metrics 回归**

Run:

```powershell
python -m unittest discover -s .\code\tests -p 'test_formal_world_model_metrics_v1.py' -v
python -m unittest discover -s .\code\tests -p 'test_formal_binary_calibration_v1.py' -v
python -m compileall -q .\code\src\pi_jwm\formal_world_model_metrics_v1.py .\code\tests\test_formal_world_model_metrics_v1.py
git diff --check -- .\code\src\pi_jwm\formal_world_model_metrics_v1.py .\code\tests\test_formal_world_model_metrics_v1.py
```

Expected：既有 classification/state/system metrics 不变；新增 link probability metrics 有有限值和正确 count。

## Task 3：接入正式 post-training runner

**Files:**

- Modify: `code/scripts/run_formal_dual_graph_gpu_train_v1.py`
- Modify: `code/tests/test_run_formal_dual_graph_gpu_train_v1.py`

- [x] **Step 1：写正式 runner RED 测试**

扩展现有 `1/1/1` CPU fixture test，断言 learned method 产生：

```text
metrics/pooled_gru__link_probability_calibration.json
```

并检查：

```python
self.assertEqual("inverse_temperature_v1", sidecar["calibration_method"])
self.assertEqual("calibration", sidecar["temperature_fit_split"])
self.assertEqual("calibration", sidecar["threshold_selection_split"])
self.assertFalse(sidecar["validation_used_for_fit_or_selection"])
self.assertTrue(sidecar["decision_equivalence_to_legacy"])
self.assertEqual([0.1, 0.3, 0.5, 0.7, 0.9], sidecar["legacy_raw_thresholds"])
self.assertFalse(sidecar["locked_test_accessed"])
self.assertFalse(sidecar["gpu_execution"])
```

metrics JSON 必须包含 mapped link threshold、legacy raw threshold identity 与 NLL/Brier/ECE；baseline methods 不拟合 temperature。

- [x] **Step 2：运行 RED**

Run:

```powershell
python -m unittest discover -s .\code\tests -p 'test_run_formal_dual_graph_gpu_train_v1.py' -v
```

Expected：缺少 sidecar 或字段而失败；不得因 fixture、路径或 import 错误失败。

- [x] **Step 3：最小接入 calibration-only 拟合**

修改 `_choose_event_thresholds`：收集 raw logits 与 labels；对 learned method 的 link 使用 `class_weights['link_activity']` 拟合 `InverseTemperatureCalibration`；其他事件和 RULE_METHODS 保持 legacy 路径。

选择顺序固定：

```text
1. calibration raw scores 上计算 legacy 五候选 F1；
2. raw 坐标 tie-break 选出 r*；
3. 计算 tau(r*)；
4. FormalMetricAccumulator 对 link 使用 event_probability 和 tau(r*)；
5. validation 只评价。
```

sidecar 字段严格复用设计规格。`comparison.csv` 必须新增显式字段：

```text
link_legacy_raw_threshold
link_probability_threshold
link_temperature
link_calibration_method
```

原 `threshold` 字段继续记录 legacy raw threshold，避免历史消费者误把其坐标改变；metrics JSON 的 `thresholds.link_activity` 记录实际 probability threshold，并用 `threshold_coordinate.link_activity='event_probability'` 明确坐标。

- [x] **Step 4：运行 GREEN 与受影响回归**

Run:

```powershell
python -m unittest discover -s .\code\tests -p 'test_run_formal_dual_graph_gpu_train_v1.py' -v
python -m unittest discover -s .\code\tests -p 'test_formal_world_model_metrics_v1.py' -v
python -m unittest discover -s .\code\tests -p 'test_formal_binary_calibration_v1.py' -v
python -m compileall -q .\code\scripts\run_formal_dual_graph_gpu_train_v1.py .\code\tests\test_run_formal_dual_graph_gpu_train_v1.py
git diff --check -- .\code\scripts\run_formal_dual_graph_gpu_train_v1.py .\code\tests\test_run_formal_dual_graph_gpu_train_v1.py
```

Expected：fixture micro-run 完成、strict reload 通过、manifest 覆盖 sidecar、`locked_test_accessed=false`。

## Task 4：建立当前 sentinel 的 CPU-only 校准审计

**Files:**

- Create: `code/scripts/run_formal_p4_link_probability_calibration_v1.py`
- Create: `code/tests/test_run_formal_p4_link_probability_calibration_v1.py`

- [ ] **Step 1：写 audit core RED 测试**

测试公开函数：

```python
build_link_probability_calibration_audit(
    calibration_logits,
    calibration_labels,
    validation_logits,
    validation_labels,
    pos_weight=50.0,
    legacy_selected_raw_threshold=0.9,
    provenance=provenance,
)
```

断言 schema、calibration/validation 分工、五候选 decision equivalence、概率指标 baseline/fitted 对照、`formal_performance_claim_ready=false`、`gpu_execution=false`、`locked_test_accessed=false`。另测 provenance 漂移、空 mask、非有限 logit、`locked_test_accessed=true` 必须失败。

- [ ] **Step 2：运行 RED**

Run:

```powershell
python -m unittest discover -s .\code\tests -p 'test_run_formal_p4_link_probability_calibration_v1.py' -v
```

Expected：新模块/函数不存在而失败。

- [ ] **Step 3：实现最小 CPU audit**

CLI 固定为：

```powershell
python .\code\scripts\run_formal_p4_link_probability_calibration_v1.py `
  --run-root '.\code\artifacts\experiments\pi_jwm_p4_edge_feedback_gru_input_gpu_20260831\seed_20260831' `
  --tensor-root '.\code\artifacts\formal_tensor\pi_jwm_v4_tensor_v4_rule_contract_h20_rb_repair_unlocked_20260827' `
  --method 'coupled_dual_gnn_residual' `
  --output-dir '.\code\artifacts\audit\pi_jwm_p4_link_probability_calibration_20260831'
```

脚本必须：

- 从 checkpoint 的 `model_config` 重建模型并 strict load；
- 只读取 sample_ids 的 calibration/validation；
- 使用同一 valid physical-edge 与 aggregate activity mask 收集 raw logits/labels；
- 从 `class_weights.json` 精确读取 train-only `pos_weight.link_activity=50.0`；
- calibration 拟合 T 和选择 raw candidate identity；
- validation 只评价；
- 输出 `link_probability_calibration_audit.json` 与 manifest；
- 保留当前 sentinel No-Go，不写新的性能通过字段。

- [ ] **Step 4：运行 GREEN**

Run:

```powershell
python -m unittest discover -s .\code\tests -p 'test_run_formal_p4_link_probability_calibration_v1.py' -v
python -m compileall -q .\code\scripts\run_formal_p4_link_probability_calibration_v1.py .\code\tests\test_run_formal_p4_link_probability_calibration_v1.py
git diff --check -- .\code\scripts\run_formal_p4_link_probability_calibration_v1.py .\code\tests\test_run_formal_p4_link_probability_calibration_v1.py
```

Expected：测试通过，audit core 不依赖 GPU 或 locked-test。

## Task 5：运行当前 sentinel CPU 验收并执行独立 Go/No-Go

**Files:**

- Generate: `code/artifacts/audit/pi_jwm_p4_link_probability_calibration_20260831/`
- Read-only: 当前 sentinel run、canonical tensor、现有 threshold replay/audit

- [ ] **Step 1：运行完整定向回归**

Run:

```powershell
$env:PYTHONPATH='D:\shen\PKU\PIJWM\code\src'
python -m unittest discover -s .\code\tests -p 'test_formal_binary_calibration_v1.py' -v
python -m unittest discover -s .\code\tests -p 'test_formal_world_model_metrics_v1.py' -v
python -m unittest discover -s .\code\tests -p 'test_run_formal_dual_graph_gpu_train_v1.py' -v
python -m unittest discover -s .\code\tests -p 'test_run_formal_p4_link_probability_calibration_v1.py' -v
python -m compileall -q .\code\src .\code\scripts .\code\tests
```

Expected：全部通过；不能用只跑新测试替代既有回归。

- [ ] **Step 2：运行冻结 sentinel CPU audit**

使用 Task 4 的固定命令。禁止改变 run、checkpoint、sample IDs、tensor、mask、pos_weight 或 threshold candidates。

- [ ] **Step 3：执行机器停止门**

必须逐项为真：

```text
decision_equivalence_to_legacy = true
selected_legacy_raw_threshold = 0.9
validation TP/FP/FN/F1 = 1902/557/5855/0.3724（允许浮点显示尾差，不允许计数差）
calibration fitted NLL <= identity NLL
validation fitted NLL/Brier/ECE <= identity counterpart
checkpoint strict missing/unexpected = 0/0
sample IDs and mask counts unchanged
manifest mismatch count = 0
gpu_execution = false
locked_test_accessed = false
formal_performance_claim_ready = false
sentinel_performance_gate = no_go
P4 = blocked
```

任一失败立即停止。不得改 loss、模型、阈值网格、拟合第二个 calibrator或启动 GPU 补救。

- [ ] **Step 4：Sol 独立复核原始 JSON**

Sol 必须亲自核对至少：`temperature`、`selected_legacy_raw_threshold`、mapped threshold、validation counts、NLL/Brier/ECE baseline/fitted、mask count、checkpoint SHA、sample IDs SHA、locked/GPU/formal flags。worker 的摘要不能替代原始 artifact。

## Task 6：同步权威记录且不夸大结论

**Files:**

- Modify: `task_plan.md`
- Modify: `progress.md`
- Modify: `findings.md`
- Modify: `记录/本地计划表.md`
- Modify: `记录/PIJWM主文档.md`
- Modify: `记录/8.12之后推进.md`
- Modify: `PROJECT_CONTEXT.md`

- [ ] **Step 1：按真实 audit 状态记录**

若 Task 5 全部通过，只允许写：

```text
probability semantics gate = passed
sentinel performance gate = no_go
P4 = blocked
formal_performance_claim_ready = false
locked_test_accessed = false
```

若概率门失败，必须记录精确失败项并停止，不得写“方案 B 已完成”。

- [ ] **Step 2：运行文档和范围检查**

Run:

```powershell
rg -n -F -e 'P4 = complete' -e 'formal_performance_claim_ready = true' task_plan.md progress.md findings.md '记录/本地计划表.md' '记录/PIJWM主文档.md' '记录/8.12之后推进.md' PROJECT_CONTEXT.md
git diff --check -- task_plan.md progress.md findings.md '记录/本地计划表.md' '记录/PIJWM主文档.md' '记录/8.12之后推进.md' PROJECT_CONTEXT.md
```

Expected：第一条无新增匹配，退出码 `1` 按“未找到夸大声明”处理；第二条退出码 `0`。

## 2. 计划自审

- [x] Spec coverage：数学、字段、split、tie-break、指标、mask、provenance、停止门、GPU/locked-test/P6 边界均有对应 Task。
- [x] 单变量：唯一拟合参数为 `T`；解析反演是固定公式，不改变模型或 loss。
- [x] 决策保护：raw candidate identity 和 mapped threshold 的逐元素等价是硬门。
- [x] 零重复：复用当前 sentinel checkpoint/tensor/sample IDs，不重复训练、阈值 replay 或方案 A 实验。
- [x] Placeholder scan：无未定义占位符或“以后补”步骤。
- [x] 类型一致性：calibration object、runner、metrics、audit 共用相同字段名。
- [x] P4 边界：概率门通过也不写 P4 完成；link-F1 No-Go 继续有效。
