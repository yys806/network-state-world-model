# PI-JWM P2-C 场景冻结与正式数据门实施计划

日期：2026-08-19

状态：正式候选数据独立验收通过；训练仍未批准

## 目标

解除 P2-C 正式数据审计的三个未冻结阻断：场景矩阵、正式规模、seed split。所有候选配置先经过 CPU-only 开发校准和机器可读审计；当前 60 条候选数据已通过独立验收，但仍不训练、不调参、不访问 locked test，下一门是训练前 tensor contract 与统计冻结。

## 固定边界

- PI-JWM 是当前主线；AirFogSim 仅作为参考模拟器和数据生成工具。
- 当前核心动作是卸载 + RB；CPU 由 `PIJWM-CPU-Inner-Rule-v1` 在每个候选通信动作后确定性分配。
- 当前 E1 只保留已验证的 5 个字段；禁止用零值、常量或代理量补齐旧 13/18 槽位。
- `D:\shen\PKU\RRM` 是独立参考项目，本轮不读取、不修改、不合并。
- 不恢复 `docs/superpowers/`，不访问 locked-test 标签。

## 阶段

| 阶段 | 状态 | 交付物 |
|---|---|---|
| 1. 现状与源码核对 | complete | AirFogSim 配置、任务生成、交通密度、UAV mobility、outage 作用点；既有校准和 P2-C 审计基线 |
| 2. 开发场景校准 | complete | 新开发 seed `910/911` 的六场景 CPU-only probe、JSON/CSV、配置与代码哈希 |
| 3. 场景矩阵冻结 | complete | 六场景数值、固定运行参数和自然 failure/outage 语义已由用户确认并写入协议 |
| 4. 正式规模与 split 冻结 | complete | 60 条轨迹、30 秒、`36/12/6/6` split、唯一 seed 规则和每场景 5/5 资源臂平衡已冻结 |
| 5. 正式入口一致性修正 | complete | v2 ledger-bound collector、资源臂元数据和 attempt/reject 来源已接入并通过真实 smoke |
| 6. P2-C 审计门补齐 | complete | protocol freeze audit 机器报告已发布并通过，三项冻结门全部 true |
| 7. 正式 v4 数据生成 | complete | v2 collector 已生成 60 条候选轨迹；独立数据验收通过，`formal_data_approved=true`，训练仍未批准 |
| 8. 训练前张量与统计冻结 | pending | 只使用 train/validation/calibration；先冻结 tensor contract、训练统计和一致性审计，再讨论训练 |

## 验收门

1. 六个场景均有完整开发重复，任务负载随 low→medium→high 单调，网络节点规模随 sparse→dense 单调，观测非空。
2. 场景数值、节点数量、UAV mobility、自然 failure/outage 观察范围和资源臂语义在正式 JSON 配置中逐项声明；未观察项标记 `not_frozen` 或 `not_modeled`。
3. 正式轨迹配额和 split seed 互斥、按场景分层，locked-test 只写 manifest 身份，不读取标签。
4. 每个动作尝试都有明确 accepted/rejected 来源，拒绝率分母可重算；不得由 quarantine=0 推断 rejection=0。
5. theory/code/data/metric consistency audit 通过后，才允许正式 v4 采集或训练。

## 本轮验证

- `test_calibrate_formal_airfogsim_scenarios_v1.py`: 2/2 passed
- `test_formal_airfogsim_dataset_v1.py`: 6/6 passed
- `test_formal_airfogsim_runtime_v1.py`: 3/3 passed
- `python -m compileall -q .\\code\\src .\\code\\scripts`: passed
- `git diff --check`: passed
- `test_formal_airfogsim_protocol_audit_v1.py`: 2/2 passed；用户冻结协议、代码 specs、校准证据哈希和安全标志独立重算通过
- `run_formal_airfogsim_protocol_audit_v1.py`: `audit_ready=true`；机器报告已发布到 `code/artifacts/audit/pi_jwm_p2c_formal_protocol_audit_20260819/`

## 错误记录

| 错误 | 处理 |
|---|---|
| 旧工作日志中的计划仍指向仓库迁移阶段 | 不覆盖历史；建立本轮独立计划并在工作日志追加当前阶段 |
| 新 seed 校准导入 AirFogSim 时 `ModuleNotFoundError: No module named 'traci'` | 已停止本次运行；核对可用 conda/解释器和项目依赖后，从正确环境重试，不修改第三方源码 |
| `airfogsim` 环境直接运行时第三方 `channel_manager_cp.py` 的 emoji 输出触发 `UnicodeEncodeError: gbk` | 使用既有验证约定 `PYTHONUTF8=1`；不修改第三方源码 |
| 资源臂平衡检查首条 Python one-liner 因 PowerShell 引号组合解析失败 | 无文件副作用；改用简单变量表达式重跑 |
| 直接使用 `code.tests...` 运行 unittest 被标准库 `code` 模块遮蔽 | 无代码副作用；改用仓库约定的 `unittest discover -s .\\code\\tests` |
| protocol audit 首版把 failure 语义和 split 配额从错误的候选节点读取 | 测试暴露后改为读取 `fixed_runtime_proposal.failure_outage_semantics` 与 `formal_scale_proposal.total_split_counts`；未修改冻结数值 |
