# PI-JWM仓库目录迁移验收

日期：2026-08-16

## 结论

本次迁移完成仓库组织层面的重构，未改变PI-JWM理论、方法、数据批准状态或实验结论。现行根目录统一为`code/`、`记录/`、`paper/`、`literature/`、`meeting/`和`docs/`；旧`代码/`、`文档/`不存在。后续项目记录只更新`记录/`。

验收所称“通过”仅指目录迁移未丢文件、未改坏内容、未新增证据门错误。P2-B/P2-C四门仍有迁移前已存在的历史source缺失或published/fresh差异，不能表述为方法门通过。

## 文件与目录

迁移映射共16,846个文件：

| 类别 | 文件数 | 新入口 |
| --- | ---: | --- |
| code | 15,290 | `code/` |
| literature | 89 | `literature/` |
| meeting | 1,365 | `meeting/` |
| records | 81 | `记录/` |
| docs | 18 | `docs/` |
| paper | 3 | `paper/` |

迁移后校验结果为：0个source残留、0个target缺失、0个大小差异、0个SHA-256差异。现行配置和入口使用新路径；历史manifest、CSV、JSON和接续记录中的旧路径作为provenance保留，通过精确映射兼容读取，不做宽泛别名。

## 证据门

四门迁移前后均退出1，差分结果均为`layout_induced=[]`：

| 门 | 当前保留的真实失败边界 |
| --- | --- |
| P2-B v1 | 已删除的v4 collector superpowers计划source |
| P2-B v2 candidate | 已删除的superpowers历史source |
| P2-C v1 | 已删除的scale audit superpowers计划source |
| P2-C v2 pre-document | 已删除的历史source及既有published/fresh差异 |

没有恢复`docs/superpowers/`，没有创建旧目录链接，也没有把兼容解析结果冒充完整理论或方法实现。

## 测试

- `python -m compileall -q .\code\src .\code\scripts .\code\tests`：退出0。
- 全仓`unittest discover`：运行1,360项，0个断言失败，7个错误；7项均因当前Python环境缺少`traci`，在导入真实AirFogSim时停止。
- 迁移路径、审计脚本、项目配置、归档审计、CPU预检和P2-C定向测试均通过。
- `git diff --check`：退出0。

上述结果不能合并表述为“全量测试全绿”。真实AirFogSim相关7项需在具备TraCI依赖的环境中复跑。

## 文献与本地资料

`literature/`含7个主分类、78个PDF、78个唯一SHA-256、78条CSV索引和78条checksum；PDF签名、索引路径和索引哈希均0错误，`待整理`不存在。文献索引与状态文件在目录迁移中保持原字节，历史Zotero键只作来源标识。

`meeting/`按原相对层次迁移；`paper/`只承接正式旧论文稿；模板、项目说明和原入口README分别进入`docs/templates/`、`docs/project-notes/`和`docs/archive/`。`code/artifacts/tmp/gpu_upload_eff9385/`因包含历史归档、补丁和压缩包而保留，不按纯缓存删除。

## 边界

- `D:\shen\PKU\RRM`是独立项目，本次未读取其论文库、未移动文件、未合并声明。
- 本次未启动GPU、未访问locked test、未修改AirFogSim第三方源码。
- 本次本地提交完成前不推送GitHub；推送需另行明确执行。
