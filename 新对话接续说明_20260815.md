# PI-JWM 新对话完整接续说明（2026-08-15）

> 用途：本文件随项目目录迁移，用于让新对话从当前真实状态继续工作。
>
> 本文件不是要求新对话无条件继承旧判断。新对话必须按本文给出的顺序重新读取代码、测试、机器可读 artifact 和 manifest；若本文与更直接、更新的证据冲突，以可复核证据为准，并记录修正。
>
> 本文中的 `<ROOT>` 表示迁移后的 PI-JWM 工作区根目录。迁移前它是 `D:\shen\网络组`。不要在新机器或新路径上机械复用旧绝对路径。

## 0. 新对话先读这一页

### 0.1 当前一句话状态

PI-JWM 已完成旧18槽信息边问题的独立审计、v4最小可靠信息边协议、CPU内层规则、非训练全双图采集器预检和联合动作 Attempt/Reject Ledger v1；目前停在 **P2-C正式数据场景矩阵、规模与split冻结之前**。正式v4数据尚未生成，模型尚未按新协议重训，真正的逐候选世界模型Rollout规划器尚未实现，不能启动100k训练。

### 0.2 当前最重要的真实结论

1. 旧数据协议虽然预留18个信息边槽位，但54条非锁定旧v3轨迹中只有索引`0/1/8/11/12`五维有真实有效计数，其余13维有效计数为0。旧R1/R2的“完成”只表示旧协议下的工程验收，不表示18维真实信息已经完成。
2. 当前不是去“补满13维”。v4协议采用最小可靠集：E1固定5个可核验字段，缺失值必须为`null + Mask + MissingReason`；旧13/18槽占位被禁止。信息越多不是目标，后续应通过最小集/增强集消融判断额外信息是否值得采集和计算开销。
3. 当前核心动作只包含卸载选择和RB稀疏COO；CPU不是策略输出，而是在每个候选通信后状态、每个rollout步上真实执行的确定性工作守恒内层规则。旧R6的联合CPU动作不再代表当前核心方法。
4. 当前R6只是 belief-conditioned direct policy：它读取世界模型belief后对候选描述符直接打分，没有逐候选调用世界模型rollout。它不能被称为“基于世界模型候选Rollout的规划器”。
5. P2-B v2 ledger候选已真实记录natural-reference `120`次attempt，其中accepted `120`、rejected `0`、quarantined `0`，拒绝率为`0/120=0.0`。这关闭了P2-C v1的“拒绝率没有真实分母”问题，但不批准正式数据。
6. P2-C v2预文档闭包审计仍是`blocked`，只剩三个阻断：`scenario_matrix_not_frozen`、`formal_scale_not_frozen`、`formal_split_not_frozen`。
7. 不需要GPU。最近任务是CPU/文档/manifest证据闭包。不得访问locked test，不得生成正式轨迹，不得继续旧100k，不得修改AirFogSim第三方源码。

### 0.3 新对话第一轮必须做什么

1. 完整读取`<ROOT>\AGENTS.md`和本文件。
2. 核对迁移后的Git分支、worktree、脏工作树和artifact是否完整，不清理任何内容。
3. 按第3节读取权威资料，并直接解析P2-B/P2-C JSON、JSONL和manifest。
4. 先向用户报告“已证实事实、文档假设、冲突、风险、串并行关系和最近一步”，不要直接训练或大改代码。
5. 当前默认接续任务是最终发布审计与GitHub非强制推送；完成前不进入正式数据设计。

### 0.4 2026-08-15治理闭包更新

- 当前`main`为`e5ad8e4 merge: close P2-C ledger evidence`，已包含框架/脚本/测试、研究归档、治理记录和P2 ledger/P2-C v2审计代码。
- P2-C v2文档绑定提交为`d6f776a`；预文档候选与final candidate的审计JSON、formal config逐字节相同，manifest仅更新证据文档source hash。final candidate位于本地忽略目录，不进入Git发布集。
- 合并后24个明确P1/P2测试模块运行`205 tests`，结果`OK (skipped=3)`；P2-B/P2-C v1/v2四个verifier均为`passed=true`、`errors=[]`。
- 当前仍为`formal_data_approved=false`、`training_eligible=false`、`gpu_started=false`、`locked_test_accessed=false`；正式场景矩阵、规模和split三个冻结门未通过。

## 1. 永久原则与用户要求

### 1.1 第一原则：理论—实现—证据一致

这是项目最高优先级约束：宁可慢，也不能理论一套、实现一套、表述再一套。

- 每个方法声明都必须能指向实现代码路径、准确输入/输出、测试、机器可读artifact和验收结果。
- 接口存在、tensor形状正确、模块命名相似、短smoke能启动，都不等于理论方法已经实现。
- 做不到理论定义时只有两条合法路线：实现并验证理论；或收缩理论边界并同步修改所有表述、指标和文档。
- 未实现内容必须标记为`target definition`、`candidate method`或`not implemented`。
- 不能用Mask、代理量、推导值、零占位、重命名或手写汇总冒充真实观测。
- 失败run、被拒绝记录、旧候选和负面结果要保留，不能为了结论整洁而删除。

### 1.2 用户与老师已确认的研究方向

- 不必追求信息边特征越多越好。少而可靠的信息若性能相近、训练/推理更快，应优先采用。
- 后续可引入不完整的真实数据，用于预训练、微调或迁移验证；必须先统一数据契约、字段证据等级和split，不能把部分真实数据冒充完整真值。
- 各模块方法不能只凭结果选中，还要说明为何适合无线网络、严格双图、稀疏事件、任务DAG、硬约束与多步决策场景，并给出文献或理论依据。
- 模块和超参数必须在统一数据、预算、seed、指标与停止规则下调优；旧候选没有经过足够调参，不能提前定型。
- 方法真正冻结后再输出完整指标、复现baseline并总结创新；阶段候选不能描述成最终方法。

### 1.3 禁止事项

- 未经一致性门批准，不启动长GPU任务。
- 最终配置冻结前不访问locked test；历史访问记录也不能被重置或忽略。
- 不从旧R6 checkpoint直接续训100k。
- 不清理主工作树，不还原不属于当前任务的修改，不覆盖已有artifact。
- 不修改`代码/reference/AirFogSim/`第三方源码来迎合协议；必要适配写在PI-JWM侧。
- 不把AirFogSim称为研究框架。AirFogSim只是仿真器、数据源和闭环执行环境。
- 不把P2 ledger称为规划器，不把P2-B preflight称为正式数据集，不把P2-C候选配置称为已批准方案。

## 2. 项目身份与理论边界

### 2.1 项目主线

PI-JWM全称Physical-Information Joint World Model，主线是：

1. 构建物理网络和信息网络的严格双图表示；
2. 建立动作条件的联合状态预测和多步rollout模型；
3. 对每个合法候选动作或动作序列真实调用冻结世界模型；
4. 使用候选相关的未来状态、任务结果、代价、风险和不确定性选动作；
5. 在AirFogSim执行首动作，采集新真实观测并滚动重规划；
6. 通过统一预测、闭环、可靠性、效率和泛化指标与baseline公平比较。

### 2.2 严格双图

- 物理图：真实硬件实体及空间、运动、环境关系。无线信道、RB、SINR和速率不属于物理边。
- 信息图：附着在物理设备上的通信/计算/服务代理，以及真实有向通信接口。任务和DAG不是信息节点或通信边。
- `C^{IP}`：信息代理到物理设备的附着关系。
- `C^{EP}`：信息通信边到有序物理端点关系的对应。
- `C^{FL}`：逻辑数据流到实际信息链路/承载hop的关系。
- 任务DAG是业务辅助结构，不能与通信flow边混成同一种边。

### 2.3 真正的世界模型规划器

目标规划器必须对同一起始belief下的每个合法候选执行动作条件rollout，读取候选相关的未来显式状态、任务完成/失败、时延、吞吐量、能耗、资源、公平性、约束风险和不确定性，再选择第一步动作并在真实反馈后重规划。

以下内容都不构成该规划器：

- 只将world-model belief拼到策略输入；
- 只对人工候选描述符直接打分；
- 只证明存在`rollout()`接口但线上选择没有调用；
- 只比较代理reward而不读取候选未来预测；
- P2采集器或Attempt/Reject Ledger。

## 3. 新对话首轮读取顺序

不要一次把全部历史读入上下文。按以下顺序建立独立理解：

1. `<ROOT>\AGENTS.md`
2. `<ROOT>\新对话接续说明_20260815.md`
3. `<ROOT>\本地计划表.md`顶部的2026-08-12以后状态；R0-R9旧表只能作为历史
4. `<ROOT>\文档\组会\2026-08-11-PI-JWM技术细节与答辩问答.md`
5. `<ROOT>\文档\组会\2026-08-11-PI-JWM组会汇报讲稿.md`
6. `<ROOT>\文档\组会\PI-JWM_组会汇报.pptx`，只理解当时汇报，不作为实现证据
7. `<ROOT>\文档\知识库\PIJWM主文档.md`：固定理论、问题定义、方法与实验规范
8. `<ROOT>\文档\知识库\8.12之后推进.md`：8月12日后的动态推进；2026-08-15已同步P2-C v2 ledger闭包后的三个剩余阻断
9. `<ROOT>\文档\研究进展\2026-08-12-PI-JWM-v4信息边均衡协议设计.md`
10. `<ROOT>\文档\研究进展\2026-08-13-PI-JWM-P1-MVS实施计划.md`
11. `<ROOT>\文档\研究进展\2026-08-13-PI-JWM-P1-A-CPU动作边界冻结设计.md`
12. `<ROOT>\文档\研究进展\2026-08-14-PI-JWM-P2-C正式数据规模与分布审计.md`
13. 隔离分支中的`文档\研究进展\2026-08-14-PI-JWM-P2联合动作Attempt-Reject-Ledger-v1实施与证据.md`
14. 第8、9节列出的核心代码、测试和artifact
15. 有具体冲突时，再回查R1-R6设计、实施、结果文档和旧PPT

知识库文档当前路径：

```text
<ROOT>\文档\知识库\PIJWM主文档.md
<ROOT>\文档\知识库\8.12之后推进.md
```

这两个文件已于2026-08-15从原外部知识库迁入当前仓库。后续只维护上述仓库内版本；不得继续写入原目录，也不能因路径变化而使用旧摘要替代文件回读。

## 4. 证据权威层级

同一问题冲突时，优先级为：

1. 用户和老师最新明确反馈；
2. 当前执行代码、测试、原始配置、JSON/JSONL/CSV、checkpoint和manifest；
3. 最新研究进展证据文档；
4. `文档\知识库\8.12之后推进.md`；
5. `文档\知识库\PIJWM主文档.md`中的固定理论边界；
6. `本地计划表.md`；
7. PPT、讲稿、旧接续说明和早期结果文档。

机器证据也不是自动可信：必须复算哈希、检查输入闭包、检查生成代码版本、确认split和数据资格，不能只看`passed=true`。

## 5. 18槽、5维有效、13维无效问题的最终处理口径

### 5.1 已证实的旧v3事实

- 旧`tensor_contract.json`声明18个信息边槽位。
- 54条非锁定轨迹中只有槽位`0/1/8/11/12`出现有效Mask计数。
- 其余13维有效计数为0；零值是占位，不是真实测量。
- 因此旧R1/R2可称“旧v3协议与评价接口通过当时验收”，不能称“18维真实信息边已经完成”。

### 5.2 当前v4方案

P1建立了29字段registry和证据等级，不要求每条边把全部字段填满。P2-B当前E1核心行宽固定为5：

| 字段 | 语义 | P2-B v2有效/无效 |
| --- | --- | ---: |
| `channel_attenuation_mean_db` | 当前决策前信道衰减均值 | 4392 / 1368 |
| `channel_attenuation_std_db` | 当前决策前信道衰减标准差 | 4392 / 1368 |
| `prev_active_flow_count` | 上一槽活动flow数 | 5472 / 288 |
| `prev_effective_rate_per_s` | 上一槽有效速率 | 5472 / 288 |
| `prev_served_data` | 上一槽已服务数据量 | 5472 / 288 |

- CSI无效原因是`CURRENT_CSI_UNAVAILABLE`；上一槽字段首帧无效原因是`NO_HISTORY`。
- invalid值必须为`null`，Mask为无效，MissingReason非空。
- `legacy_placeholder_field_count=0`，每条E1记录row width严格为5。
- 动作采用`(time, flow, edge, RB)`稳定身份的稀疏COO，不用RB count或bitmap代替真实动作身份。
- E2/E3只允许接入具有同RB身份和正确采样时刻的直接证据；没有证据就不实现。

### 5.3 老师“少信息也可以”的落实方式

老师意见已经进入当前协议，不是待办口号：

- 最小可靠集E1先作为核心输入；
- 不再为了保持18维形状而补造13维；
- 后续只在正式数据完成后做预注册消融：E1最小集、E1+可恢复增强、E1+新采集增强；
- 同时比较预测/闭环性能、稳定性、训练时间、推理时延、显存和缺失鲁棒性；
- 若增强字段没有稳定收益，最终方法应保留较小字段集。

这会使旧模型结果失去“当前最终方法”资格，但不会删除其历史工程价值。正式v4输入、归一化、数据分布和动作定义变化后，相关模型必须重新训练；不能把旧checkpoint直接当新协议checkpoint。

## 6. CPU动作边界与旧实验失效范围

当前核心动作：

```text
candidate action = offloading choice + RB sparse COO
```

候选应用流程：

```text
合法性检查
-> 应用卸载/RB setter
-> 完成本槽通信更新
-> 对候选后的任务集合逐节点执行 PIJWM-CPU-Inner-Rule-v1
-> 更新任务、队列、能耗和双图状态
```

CPU规则必须逐候选、逐rollout步真实调用；不能预先固定一个与候选无关的CPU结果。旧R1中的混合CPU策略数据和旧R6中的学习CPU动作保留为历史扩展动作证据，不用于当前核心方法结论。旧R6 10k的18个run不能直接续100k。

## 7. 阶段状态

| 阶段 | 当前真实状态 | 能证明什么 | 不能证明什么 |
| --- | --- | --- | --- |
| P0一致性审计 | 已完成首轮核查 | 找出18/5/13、规则层和规划器等冲突 | 不等于所有冲突已修复 |
| P1信息边MVS | 已通过 | registry、MissingReason、旧槽迁移和非锁定审计可执行 | 不等于正式v4数据完成 |
| P1-A CPU边界 | 已冻结 | 核心动作=卸载+RB，CPU=确定性内层规则 | 不等于世界模型已按此重训 |
| P2-A CPU规则 | 已通过CPU/interface门 | 规则守恒、确定性和callback接口一致 | 不等于完整轨迹或正式数据完成 |
| P2单步/多步 | 非训练smoke通过 | setter前CSI、跨槽E1回填、词表和原子发布可执行 | 不等于全覆盖采集器或训练集 |
| P2-B v1 | 已合并main | 6条自然episode、120帧、严格双图/任务/flow覆盖和replay | 没有完整attempt/reject分母 |
| P2-C v1 | 审计完成但blocked | 发现并恢复83项AirFogSim source closure，冻结正式数据缺口 | 不批准正式场景、规模和split |
| P2-B v2 ledger | 隔离分支候选通过 | 真实attempt状态机、角色分离、拒绝率分母 | 不等于正式数据，不是规划器 |
| P2-C v2 | 预文档闭包候选为blocked | ledger门、frame/replay门通过，只剩3项阻断 | 尚未完成最终文档绑定闭包 |
| P3-R5旧世界模型 | 历史候选证据 | 旧协议下的模块接口、筛选和负面结果 | 不是新协议最终模型 |
| R6旧策略 | 18组10k历史工程证据 | direct policy闭环和安全壳可运行 | 没有逐候选WM rollout，未收敛、未定型 |
| P4/P5/P6新模型与规划器 | 未实施/阻塞 | 只有target definition | 不能声称方法已完成 |
| 100k、baseline、最终指标 | 暂不开展 | 无 | 不得启动或对外宣称 |

## 8. 当前Git和worktree状态

以下8.1和8.2是治理前的迁移快照；当前状态见8.4。迁移前核验时间：2026-08-15。

### 8.1 主工作树

- 路径：`D:\shen\网络组`
- 分支：`main`
- HEAD：`7d858336802da9e6c6ed071fcdd7d13fe5eb122f`
- 相对远端：`main...origin/main [ahead 167]`
- 工作树很脏，包含大量既有修改、删除和未跟踪文件。不得执行`git reset --hard`、`git checkout --`或批量清理。
- `本地计划表.md`也有用户改动；后续只能读取真实内容后做最小补丁。

### 8.2 当前P2 ledger隔离worktree

- 路径：`<ROOT>\.worktrees\p2-action-attempt-ledger-v1`
- 分支：`codex/p2-action-attempt-ledger-v1`
- HEAD：`abfbe1077e612ed8deb5a43f72c4c69170c76b41`
- 基线：`7d85833 docs: plan P2 action attempt ledger`
- 新增提交：

```text
7af39ca feat: add joint action attempt ledger
4701aa5 feat: observe v2 collector runtime attempts
a025045 feat: publish ledger-bound P2 artifacts
5826087 feat: collect role-separated P2 attempt evidence
5a08bde feat: audit P2 rejection rate from ledger
abfbe10 docs: record P2-B v2 ledger evidence
```

- 当前未提交修改只有证据文档两处行尾空格清理。首次提交前检查曾发现这两处空格，但PowerShell流程仍执行了commit；随后已在工作树修正。必须重新独立运行`git diff --check`并确认返回0，不能把首次检查描述为通过。
- 上述分支状态属于治理前快照；该分支已在`e5ad8e4`以非破坏性merge方式合并到`main`。

### 8.3 其他隔离worktree

迁移前还存在：

```text
.worktrees/p2-c-scale-distribution-audit
.worktrees/sparse-event-diagnostic-v2
```

P2-C v1分支已经通过merge commit进入main；`sparse-event-diagnostic-v2`不是当前接续主线。不要删除这些worktree，也不要误在错误分支继续P2 ledger。

### 8.4 治理后主工作树状态

- 路径：`D:\shen\PKU\PIJWM`
- 分支：`main`
- HEAD：`e5ad8e494ebb9a833ed155eeb4d53d85af9dcc72`
- 相对远端：治理发布已通过非强制push同步，交付前远端ref与本地`main`复核一致。
- 三个迁移worktree均无`prunable`记录；P2 ledger AirFogSim junction指向当前`代码/reference/AirFogSim`。
- 仓库治理与GitHub发布已完成；下一研究步骤仍需先冻结正式场景矩阵、规模和split，正式数据、训练、GPU和locked-test仍禁止。

## 9. P2-B v2实现与机器证据

### 9.1 核心代码

位于`codex/p2-action-attempt-ledger-v1`：

```text
代码/src/pi_jwm/action_attempt_ledger_v1.py
代码/src/pi_jwm/airfogsim_full_dual_graph_collector_v2.py
代码/src/pi_jwm/full_dual_graph_artifact_v2.py
代码/src/pi_jwm/p2c_scale_distribution_audit_v2.py
代码/scripts/run_p2_full_dual_graph_collector_preflight_v2.py
代码/scripts/run_p2c_scale_distribution_audit_v2.py
```

对应测试：

```text
代码/tests/test_action_attempt_ledger_v1.py
代码/tests/test_airfogsim_full_dual_graph_collector_v2.py
代码/tests/test_full_dual_graph_artifact_v2.py
代码/tests/test_run_p2_full_dual_graph_collector_preflight_v2.py
代码/tests/test_p2c_scale_distribution_audit_v2.py
代码/tests/test_run_p2c_scale_distribution_audit_v2.py
```

### 9.2 Attempt定义与状态机

一个attempt是一个时隙提交给执行链的一份完整联合动作候选。当前每帧只有`candidate_ordinal=0`。

成功状态机：

```text
begun
-> candidate_built
-> contract_validated
-> pre_setter_revalidated
-> setters_applied
-> env_step_started
-> env_step_completed
-> outcome_captured
-> accepted
```

mutation状态：`none`、`confirmed`、`unknown_after_runtime_call`。v2只通过实例级scheduler proxy、`env.step`包装和observer包装观察现有v1 executor，所有包装在`finally`恢复，不做module-global monkeypatch。rejected attempt阻断成功发布，不换候选重试。

### 9.3 P2-B v2候选

```text
<ROOT>\代码\artifacts\preflight\pi_jwm_p2_full_dual_graph_collector_v2_candidate_20260814
```

机器解析事实：

| role | attempts | accepted | rejected | quarantined |
| --- | ---: | ---: | ---: | ---: |
| natural_reference | 120 | 120 | 0 | 0 |
| natural_replay | 120 | 120 | 0 | 0 |
| bootstrap | 10 | 10 | 0 | 0 |
| fixture | 10 | 10 | 0 | 0 |

- 总attempt `260`，发布frame `120`。
- natural-reference/frame key差异0；reference/replay digest差异0。
- 非零`candidate_ordinal`为0。
- `env_step_called=false`和`env_step_completed=false`均为0。
- setter调用明细627条。
- `training_eligible=true`为0。
- 成功bundle包含9个required files、8个artifact hash、126个source hash。
- 6/6 fresh-environment replay通过。
- `formal_data_approved=false`、`v4_dataset_complete=false`、`model_training_started=false`、`candidate_rollout_planner_complete=false`。

## 10. P2-C v2当前状态

### 10.1 预文档闭包候选

```text
<ROOT>\代码\artifacts\audit\pi_jwm_p2c_scale_distribution_audit_v2_pre_document_closure_20260814
```

真实受管文件只有：

```text
p2c_scale_distribution_audit_v2.json
p2c_formal_data_config_candidate_v2.json
manifest.json
```

不要引用不存在的`audit_report.json`或`formal_dataset_candidate_config.json`。

机器报告：

- `audit_status=blocked`
- natural-reference attempts/accepted/rejected：`120/120/0`
- rejection rate：`0.0`
- quarantined：`0`
- ledger schema/transition gate：通过
- frame/replay alignment gate：通过
- `formal_data_approved=false`
- 阻断项只有：

```text
formal_scale_not_frozen
formal_split_not_frozen
scenario_matrix_not_frozen
```

该候选生成时曾通过独立`--verify-only`。随后证据文档清理了两处行尾空格，改变了被manifest绑定的源文件字节；因此它只能保留为`pre_document_closure`历史候选。最终候选必须在证据文档更新并提交后重新生成，不能覆盖这个目录。

### 10.2 尚不存在的最终候选

目标目录：

```text
<ROOT>\代码\artifacts\audit\pi_jwm_p2c_scale_distribution_audit_v2_candidate_20260814
```

截至本文件创建时该目录尚不存在。不得在交接中把它写成已生成或已验证。

## 11. 已有测试与验证边界

使用环境：

```text
D:\miniconda\envs\airfogsim\python.exe
PYTHONUTF8=1
PYTHONPATH=<ROOT>\代码\src
```

已有证据：

- P2 ledger候选生成前，24个P1/P2模块实际展开`203/203`通过。
- P2-B v1、P2-C v1历史`--verify-only`通过。
- AirFogSim manifest依赖`83/83` SHA-256匹配。
- v2各切片均按实际RED->GREEN开发。
- P2-B v2候选发布后验证与独立verify曾通过。
- P2-C v2预文档候选生成时verify曾通过。

严格表述边界：当前证据文档字节已变化，最终闭包完成前应重新跑全部203项和四个verify，不能用历史通过替代最终验收。历史一次全仓测试曾展开933项并有283个环境/历史模块错误，因此不得声称“全仓测试全部通过”；当前应使用冻结的focused suite和明确环境复现。

## 12. 最近一个最小可验证步骤

### 12.1 目标

完成P2-C v2“证据文档 -> manifest -> 最终候选”闭包，只关闭ledger证据链，不冻结正式数据场景、规模或split。

### 12.2 执行顺序

工作目录：

```text
<ROOT>\.worktrees\p2-action-attempt-ledger-v1
```

1. 更新`文档\研究进展\2026-08-14-PI-JWM-P2联合动作Attempt-Reject-Ledger-v1实施与证据.md`：写入P2-C v2预文档候选的真实数字和三个剩余阻断；明确最终文档绑定候选尚未生成。
2. 单独运行`git diff --check`，必须返回0。
3. 确认staged文件只有该证据文档，再提交文档闭包。不要把其他文件顺带提交。
4. 在`代码\scripts`运行：

```powershell
$env:PYTHONUTF8='1'
$env:PYTHONPATH='<ROOT>\代码\src'
& 'D:\miniconda\envs\airfogsim\python.exe' run_p2c_scale_distribution_audit_v2.py `
  --bundle '<ROOT>\代码\artifacts\preflight\pi_jwm_p2_full_dual_graph_collector_v2_candidate_20260814' `
  --output-dir '<ROOT>\代码\artifacts\audit\pi_jwm_p2c_scale_distribution_audit_v2_candidate_20260814'
```

5. 对同一命令加`--verify-only`独立复验。
6. 比较预文档候选与最终候选：audit JSON和candidate config应字节相同；manifest允许仅因证据文档hash变化而不同。不得删除预文档候选。
7. 再运行完整focused 203项、P2-B v2/P2-C v2/P2-B v1/P2-C v1四个verify、AirFogSim 83/83、Python编译和`git diff --check`。
8. 确认v1生产文件、v1 artifact和AirFogSim第三方目录diff为空。

### 12.3 验收标准

全部满足才可称“P2-C v2 ledger证据闭包完成”：

- 最终候选目录原子生成，3个required files齐全；
- `--verify-only`返回通过且错误列表为空；
- audit/config与预文档候选字节一致；
- manifest source hash绑定新提交后的证据文档；
- natural-reference仍严格为120 attempts = 120 accepted + 0 rejected，quarantined=0；
- 三个场景/规模/split阻断原样保留；
- `formal_data_approved=false`、`training_eligible=false`、`gpu_started=false`、`locked_test_accessed=false`；
- focused suite与历史v1验证均通过；
- 没有修改AirFogSim或旧artifact。

该闭包已完成；`本地计划表.md`和`文档\知识库\8.12之后推进.md`均应保持P2-C只剩场景、规模、split三个阻断。同步时读取主工作树的真实版本并做最小补丁，不能从隔离worktree覆盖主文件。

## 13. P2-C闭包之后的串行路线

必须按顺序：

1. 冻结正式scenario matrix：任务负载、网络密度、节点presence/移动性、failure/outage和资源臂。
2. 依据场景分层、稀有事件覆盖和统计功效推导最小episode数/步数；不能先拍规模再补理由。
3. 冻结train/validation/calibration/locked-test seed split和一次性locked-test规则。
4. 冻结正式生成配置、拒绝率/隔离/replay/source closure门；仍保持`formal_data_approved=false`直到所有门通过。
5. 在新目录生成正式v4数据，原子发布、失败隔离、manifest闭包；不覆盖v3。
6. 重构正式数据集、严格双图张量、归一化、窗口和评价映射。
7. 审计P3/P4世界模型理论缺口，特别是逐步确定性规则层、动态presence、DAG/flow传播和动作条件rollout。
8. 在新数据上完成CPU门、小规模GPU模块/超参数筛选和多seed组合复验，冻结工作世界模型。
9. 实现P6真正逐候选世界模型Rollout规划器，并与规则搜索、direct policy等清晰对照。
10. 训练策略器和规划器，输出预测、闭环、风险、效率、资源和统计指标。
11. 复现完整论文baseline，统一状态、动作、reward、预算、seed、停止规则和指标。
12. 只有方法、超参数、数据和报告模板冻结后，才一次性访问locked test并定型最终方法。

## 14. 可以并行与暂不开展

### 14.1 可并行但不能改变P2串行门

- 整理baseline候选、开源代码、许可证、输入输出和公平对比矩阵；只做设计/CPU可复现性准备，不启动正式训练。
- 调研真实数据来源，建立字段、单位、采样频率、时间对齐、缺失机制和隐私/许可证矩阵；不把未对齐数据并入训练。
- 为E1最小集、可恢复增强集和新采集增强集预注册消融与开销指标。
- 完善43项评价协议到新v4字段和闭环输出的映射，标记可直接计算、需后处理、需闭环和N/A。
- 整理模块方法的文献依据和场景适配理由；Zotero是权威文献库，新增前去重。
- 补充CPU单元测试、失败路径、manifest verifier和数据统计脚本。

### 14.2 暂不开展

- 正式v4轨迹生成；
- 任何新模型长GPU训练或旧100k续训；
- baseline正式训练；
- locked test；
- 最终模型/规划器/创新点冻结；
- 用真实数据微调或预训练；
- 对外报告最终优越性。

## 15. 关键artifact入口

```text
代码/artifacts/audit/pi_jwm_p1_information_edge_contract_v4/
代码/artifacts/preflight/pi_jwm_cpu_inner_rule_v1/
代码/artifacts/preflight/pi_jwm_p2_single_step_collector_v1/
代码/artifacts/preflight/pi_jwm_p2_multistep_collector_v1/
代码/artifacts/preflight/pi_jwm_p2_full_dual_graph_collector_v1/
代码/artifacts/audit/pi_jwm_p2c_scale_distribution_audit_v1/
代码/artifacts/preflight/pi_jwm_p2_full_dual_graph_collector_v2_candidate_20260814/
代码/artifacts/audit/pi_jwm_p2c_scale_distribution_audit_v2_pre_document_closure_20260814/
```

历史模型/策略证据只在需要做影响审计时读取：

```text
代码/artifacts/formal_training/pi_jwm_r4_gpu_screening_v1/
代码/artifacts/formal_training/pi_jwm_r5_gpu_training_v1/
代码/artifacts/formal_training/pi_jwm_r5_multi_seed_analysis_v4/
代码/artifacts/formal_training/pi_jwm_r5_module_confirmation_v1/
代码/artifacts/formal_training/pi_jwm_r6_gpu_training_v3/
代码/artifacts/analysis/pi_jwm_r6_10k_gate_analysis_v1/
```

## 16. 新对话首轮汇报模板

完成读取后，先向用户回答以下五项，不要只复述本文：

1. 独立核查后的项目现状：当前停在哪个门、哪些路径真实可执行。
2. 已证实事实、文档假设和冲突：至少覆盖18/5/13、CPU动作、P2 ledger、P2-C三阻断和规划器缺口。
3. 当前最关键风险：证据闭包、正式场景/split规模、旧模型语义不兼容、稀疏事件和理论实现缺口。
4. 必须串行、可并行、暂不开展的工作。
5. 最近一个最小可验证步骤及验收标准：默认就是第12节，不启动GPU。

如发现本文任何数字或路径与JSON、JSONL、代码或manifest不一致，应立即以直接证据修正本文和动态计划，并说明是否会使已有实验失效。不能为了“顺利接上”而隐藏差异。

## 17. 迁移完整性检查

文件夹迁移后先做只读核验：

```powershell
git status --short --branch
git worktree list --porcelain
git log --oneline --decorate --all -20
```

还应确认：

- `.git`、`.worktrees`和LFS/大artifact是否真的随迁；
- P2-B v2候选9个文件和P2-C v2预候选3个文件存在；
- `代码/reference/AirFogSim/`的83项manifest依赖仍可复算；
- `D:\miniconda\envs\airfogsim\python.exe`在新环境是否仍存在；若路径变化，只更新运行环境，不改证据含义；
- 外部知识库两份Markdown是否另外迁移；
- 主工作树脏状态与迁移前一致，不因复制工具静默丢失未跟踪文件、空目录、junction或混合换行文件。

特别注意：`energy_manager.py`的manifest目标字节包含历史混合换行。迁移工具若自动规范换行，会破坏SHA-256。出现hash mismatch时先核对迁移字节，不要修改第三方逻辑或重新生成一个“看起来相同”的文件冒充原证据。
